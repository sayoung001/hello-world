# 📈 한·미 주식 반자동 추천 시스템 — 통합 설계 및 착수 전 검토

> 목적: 기존 `US_KR_)CLAUD`(주식 분석) + 코인 자동매매 봇(`auto_trader_v9` 등) 패턴을 **주식 도메인으로 병합·전환**
> 작성일: 2026-06-21 · 상태: **착수 전 검토용 (구현 X)**

---

## 0. 확정된 전제 (사용자 결정)

| 항목 | 결정 |
|------|------|
| **대상 시장** | 한국(KOSPI/KOSDAQ) + 미국(NASDAQ/NYSE) |
| **일일 배치 기준 시각** | **미국 장 마감 + 1시간** (장외거래·선물·미국발 뉴스 반영 목적) |
| **뉴스 수집 범위** | 미국 장 마감 +1시간까지의 이슈 포함 |
| **실행 방식** | **추천 + 텔레그램 알림만 (반자동)** — 실제 주문 자동체결 없음 |
| **에이전트** | Claude API 기반 멀티 에이전트 |
| **데이터 소스** | 미정 → 본 문서에서 추천 |
| **결과 정리** | 노션(Notion)으로 시각화 |

### 0.1 타이밍 재정의 (KST 기준)
미국 마감 16:00 ET = KST 06:00(표준시) / 05:00(서머타임). **+1시간 = KST 06:00~07:00**.

```
┌──────────────────────────────────────────────────────────────┐
│  [일일 배치]  US마감+1h ≈ KST 06:00~07:00                      │
│    → 한·미 일봉 다운로드 → 스크리닝 → 섹터분석                  │
│    → 에이전트 코멘트 → 종합추천 → Notion 게시 + Telegram 요약   │
│                                                              │
│  [KR 개장 모니터]  KST 09:00  → 실시간 거래량 폭주 감지·알림   │
│  [US 개장 모니터]  KST 23:30(서머 22:30) → 동일                │
└──────────────────────────────────────────────────────────────┘
```
- 배치가 KR 개장(09:00)보다 ~2시간 앞서 끝나므로 **장 시작 전 추천 준비 완료** ✅
- 미국 추천은 새벽에 생성되어 US 개장(KST 23:30)까지 ~16시간 시차 → **개장 직전(KST ~22:00) 경량 리프레시** 1회 권장(가격·뉴스 갱신).

---

## 1. 거래 형태 (어떻게 거래하는가)

본 시스템은 **주문을 내지 않는다.** 사람이 매매하도록 **의사결정을 보조**한다.

1. **유니버스 선정** — 시총·거래대금 필터로 한·미 각각 수백 종목으로 압축(전체 스캔은 성능·API 한계).
2. **스크리닝** — `US_KR_)CLAUD` 실효점수(Effective Score) + 코인봇식 레짐 게이트로 후보 추출.
3. **섹터 진단** — 후보 종목의 섹터 상태(강력매수~하락위험 5단계) 부여.
4. **에이전트 코멘트** — 통과 종목에 한해 뉴스/수급/밸류 코멘트 생성(티어드 → 비용 절감).
5. **종합 추천** — 종합판단 에이전트가 보유기간별(단타/중단기/스윙) 추천 + **진입/손절/목표 + 당일 예상 손익 범위 + 매매 방법**(언제 사고 팔지) 작성.
6. **게시** — Notion 대시보드 + Telegram 요약.
7. **개장 모니터** — 장 시작 순간 거래량 폭주 종목을 실시간 감지해 Telegram 즉시 알림.

> ⚠️ **반자동 = 투자 권유 아님.** 추천가와 실제 체결가 사이 슬리피지/갭이 발생하므로, 모든 가격은 "조건"으로 표현(예: "시가 -1% 눌림 시 매수")한다.

---

## 2. 코드 아키텍처 (어떻게 작성되는가)

### 2.1 디렉토리 구조 (제안)
```
stock_auto/
├── config/
│   ├── settings.py        # 시장별 임계값·API 키·텔레그램·Notion 토큰
│   ├── markets.py         # 거래소 캘린더, 개장/마감, DST, 휴장일
│   └── universe.py        # 유니버스 정의·필터(시총/거래대금)
├── data/
│   ├── downloader.py      # 일봉 다운로드 (코인봇 download_data.py 계승)
│   ├── realtime_feed.py   # 실시간 체결/거래량 스트리밍 (KIS WebSocket)
│   └── store/feature_store.py  # 일별 결과·피처·추천이력 저장
├── indicators/
│   └── indicators_v2.py   # US_KR_)CLAUD 계승 + ★KR 통화버그 수정
├── strategy/
│   ├── strategies.py      # US_KR_)CLAUD Effective Score 계승
│   ├── regime.py          # regime_v2 + 지수 매크로 레짐(코인봇 BTC필터 번역)
│   └── screener.py        # Stage 2 스크리닝 오케스트레이션
├── sector/
│   ├── sector_map.py      # 계승
│   └── sector_analysis.py # 계승
├── agents/                # Claude API 멀티 에이전트
│   ├── base.py            # 공통 호출·캐싱·티어링·근거주입
│   ├── news_agent.py      # 뉴스 수집·요약·센티먼트
│   ├── flow_agent.py      # 수급/거래(매수매도·외인·기관) 코멘트
│   ├── value_agent.py     # 가격/밸류(밸류에이션·기술수준) 코멘트
│   └── portfolio_agent.py # ★종합 판단 (Stage 6)
├── realtime/
│   └── volume_monitor.py  # Stage 7 개장 거래량 폭주 감지
├── notify/telegram.py     # 코인봇 텔레그램 계승
├── integrations/notion_publisher.py  # Stage 9 Notion 게시
├── pipeline/
│   ├── daily_batch.py     # Stage 1~6 오케스트레이션
│   └── scheduler.py       # US마감+1h / KR개장 / US개장 트리거
└── backtest/labeler.py    # outcome_labeler.py 계승 (검증 인프라)
```

### 2.2 코인봇 → 주식 전환 매핑
| 코인봇(auto_trade) | 주식 시스템 | 전환 방식 |
|--------------------|-------------|-----------|
| 15분봉, 24/7 | 일봉(배치) + 분봉(실시간 모니터) | 시간단위·세션 캘린더 도입 |
| 무기한선물 20x, ROE | 현물, 단순 수익률 | 레버리지 제거, 갭/상하한가 처리 |
| BTC 7단계 필터(레벨0~6) | **지수 매크로 레짐**(SPY/QQQ, KOSPI/KOSDAQ) | 시장 위험 게이트로 번역 |
| Cascade E / CasTrail D 청산모드 | **매매 가이드(조건)** | 실주문 대신 "언제 매도" 규칙으로 |
| 실주문 체결 | 추천+알림 | 주문 모듈 제외 |
| CoinGlass(펀딩·OI) | 수급(외인·기관 순매수, 공매도) | flow_agent가 대체 |
| 텔레그램 알림 | 그대로 계승 | 재사용 |

### 2.3 데이터 소스 추천
- **일봉(배치)**: `FinanceDataReader`(한·미 무료, 광범위) + `yfinance`(미국 보조). → `downloader.py`
- **실시간(개장 모니터)**: **한국투자증권(KIS) Open API** 권장 — 무료, REST + **WebSocket 실시간 체결/호가**, **국내·해외(미국) 모두 지원**. 반자동이라 주문 권한 없이 시세만 사용 가능.
  - 대안: 미국 실시간 무료는 제약 큼(지연 15분 多). KIS 해외 실시간이 현실적 최선.
- **뉴스**: (미정) 후보 — 네이버 뉴스 RSS/검색(국내), NewsAPI·Finnhub·RSS(미국), 종목 IR 공시(DART/SEC EDGAR). → 라이선스·비용 검토 후 확정.

---

## 3. 에이전트 설계 (Claude API 멀티 에이전트)

**티어드 원칙**: 수치 시그널은 규칙으로 1차 필터 → **스크리닝 통과 종목만 LLM** 투입(비용·지연·환각 최소화).
모든 에이전트는 **근거 데이터를 입력으로 주입**받아 환각을 억제하고, **출처를 명시**한다.

| 에이전트 | 입력 | 출력 | 모델 |
|----------|------|------|------|
| **news_agent** | 종목·섹터 관련 뉴스 원문/헤드라인 | 핵심 이슈 3줄 요약 + 센티먼트(-1~+1) + 출처 | Haiku(대량) |
| **flow_agent** | 외인/기관 순매수, 거래대금, 공매도, 체결강도 | 수급 해석 + 주체 판단 | Haiku |
| **value_agent** | 밸류에이션, 기술적 위치(저항/지지), 변동성 | 가격대 평가, 과열/저평가 | Haiku |
| **portfolio_agent** | 위 3개 + 섹터상태 + 종목 시그널 + 레짐 | **최종 추천 JSON**(아래) | Opus(소수 정예) |

### 3.1 종합 판단 에이전트 출력 스키마 (Stage 6)
```jsonc
{
  "ticker": "005930", "name": "삼성전자", "market": "KR",
  "recommend": "BUY",            // BUY / WATCH / AVOID
  "horizon": "중단기",            // 단타 / 중단기 / 스윙
  "conviction": 0.72,            // 0~1 (수치+에이전트 종합)
  "entry": { "rule": "시가 대비 -1% 눌림 & RVOL>1.5", "zone": [71000, 71800] },
  "stop": 69500, "targets": [74000, 76500],
  "expected_pnl_today": {        // ★ 당일 예상 손익 — 점추정 아닌 '범위'
      "p10_pct": -2.1, "p50_pct": +1.4, "p90_pct": +4.8,
      "basis": "ATR·과거 유사셋업 분포" },
  "exit_plan": "목표1 도달 50% 익절 / 종가 EMA20 이탈 시 전량 / 미달성 시 시간청산(D+5)",
  "rationale": { "sector": "...", "signal": "...", "news": "...", "flow": "...", "value": "..." },
  "sources": ["..."]
}
```
> **예상 손익은 점추정(예 "+3.2%")이 아니라 분위수 범위(P10/P50/P90)**로 표현 — 과신 방지. 근거는 ATR 기반 TP/SL 도달확률 + (백테스트 구축 후) 과거 유사 셋업 결과분포.

---

## 4. ★ 개장 거래량 "폭주" 감지 방법론 (요청 리뷰)

사용자가 제시한 두 축 — **(A) 가격이 크게 움직이는 종목**, **(B) 평소보다 훨씬 많은 거래량** — 을 모두 포함해 리뷰한다.

### 4.1 가장 중요한 함정: "시간대 정규화"
개장 직후 거래량은 **원래** 높다. 일평균 거래량과 비교하면 매일 아침 전 종목이 "폭주"로 오탐된다.
→ **반드시 같은 시간대(time-of-day) 누적 거래량과 비교**해야 한다.

### 4.2 후보 지표
| # | 지표 | 정의 | 잡아내는 것 |
|---|------|------|-------------|
| 1 | **시간대 RVOL** | 09:00~09:05 누적거래량 / 최근 20일 동시간대 평균 | (B) 상대적 거래량 폭증 ★핵심 |
| 2 | **거래량 Z-score** | (오늘 동시간대 거래량 − 평균)/표준편차 | (B) 통계적 이상치(>3σ) |
| 3 | **거래대금 하한** | 절대 거래대금 ≥ 임계(예 KR 50억/US $2M) | 소형주 노이즈 제거(필수 동반필터) |
| 4 | **시가 갭/ROC** | (현재가−전일종가)/전일종가, 분당 가격변화율 | (A) 가격이 크게 움직임 |
| 5 | **체결강도** | 매수체결량/매도체결량 (KIS 제공) | 폭주의 방향성(매수 우위) |
| 6 | **호가 불균형** | 매수잔량/매도잔량 | (A) 모멘텀 압력 |
| 7 | **틱 빈도(Tick rate)** | 단위시간 체결 건수 | (A) "티커가 많이 움직임" |

### 4.3 권장 조합 (오탐 최소화)
거래량과 가격은 **AND로 결합**해야 진짜 신호다. 거래량만 터지고 가격이 안 움직이면 분산매물/churn일 수 있다.

```
폭주_알림 =  (시간대RVOL ≥ 3.0  OR  거래량Z ≥ 3σ)        # B: 거래량
          AND |시가갭 또는 분당ROC| ≥ 임계               # A: 가격 이동
          AND 거래대금 ≥ 하한                            # 노이즈 제거
          AND 체결강도 ≥ 1.2 (선택, 매수우위)            # 방향성
```
- **시작 임계값(튜닝 전 기본값)**: 시간대RVOL ≥ 3.0, Z ≥ 3.0, 갭/ROC ≥ 한국 3%·미국 5%, 거래대금 KR≥50억·US≥$2M.
- **윈도우**: 개장 후 첫 1·3·5분 멀티 윈도우 — 너무 짧으면 동시호가 잡음, 너무 길면 늦음.
- **동시호가 처리**: KR 09:00 동시호가 체결분 별도 취급(첫 캔들 왜곡 방지).

### 4.4 오탐/운영 관리
- **쿨다운/디듀프**: 같은 종목 N분 내 재알림 억제.
- **유니버스 분리**: ① 추천 리스트 우선 모니터 + ② 전체 유니버스 폭주 스캔(둘 다, 태그 구분).
- **뉴스성 일회 스파이크 vs 지속**: 후속 캔들에서 RVOL 유지되는지 2차 확인(VAI의 "지속 vs 단발" 개념을 분봉으로 이식).
- **점진 튜닝**: 초기엔 알림만 쌓고, 사후 수익률로 임계값을 **백테스트 기반 보정**.

---

## 5. 노션(Notion) 정리 구조 (Stage 9)

매일 배치 종료 시 게시. **갱신형 DB + 일자 페이지** 혼합.

```
📒 주식 추천 대시보드 (Notion)
├── 🗓️ 일자별 페이지  (2026-06-21)
│   ├── 종합 요약 (시장 레짐, 한·미 코멘트)
│   └── 섹터 현황 보드 (12섹터 상태 5단계 색상)
├── 🗃️ DB: 오늘의 추천   (종목·시장·점수·보유기간·진입/손절/목표·예상손익·섹터상태)
├── 🗃️ DB: 거래량 폭주 로그  (시각·종목·RVOL·갭·체결강도·결과추적)
└── 📄 종목 상세 페이지 (에이전트 코멘트: 뉴스/수급/밸류 + 근거·출처)
```
- **DB 스키마**를 먼저 확정(속성·타입) → `notion_publisher.py`가 매일 upsert.
- Notion API rate limit 고려해 배치 게시(종목 상세는 추천 통과분만).

---

## 6. ⚠️ 착수 전 반드시 검토할 문제점 (전부 나열)

### 🔴 차단급 (정해지지 않으면 구현 불가)
1. **실시간 데이터 인프라 미확정** — 거래량 폭주(Stage 7)는 실시간 스트리밍 필수. 무료 미국 실시간은 지연 15분이 흔함. **KIS Open API 계좌/앱키 발급 여부**가 선행되어야 함. 없으면 Stage 7은 분봉 폴링으로 "근사"만 가능(지연·정확도 저하).
2. **뉴스 데이터 소스 미정** — 에이전트의 뉴스 코멘트는 수집원이 있어야 성립. 국내(네이버/DART)·미국(NewsAPI/Finnhub/SEC) 중 선택·비용·라이선스 확정 필요.

### 🟠 구조·정합성
3. **코인 ↔ 주식 본질 차이** — 레버리지·24/7·무기한선물 가정이 깔린 코인 로직(ROE, 즉시청산/트레일링)을 그대로 옮기면 안 됨. 갭·상하한가·거래정지·세금/수수료·신용 등 주식 고유 요소 반영 필요.
4. **BTC 7단계 필터 → 지수 레짐 번역** — 단순 이식 불가. SPY/QQQ·KOSPI·VIX 기반 매크로 레짐을 새로 정의해야.
5. **KR Liquidity 통화 버그(기존 코드 결함)** — `_calc_liquidity_score`가 항상 USD 기준. 병합 시 시장별 통화 분기 **필수 수정**(이전 분석 P0).
6. **백테스트/검증 부재** — 병합 전략의 가중치·임계값·추천 신뢰도가 전부 미검증. 추천 시스템의 근거 신뢰성에 직결. `backtest/labeler.py`(outcome_labeler 계승) + Walk-Forward 우선 구축 권장.

### 🟡 신뢰성·운영
7. **"당일 예상 손익"의 정의 모호** — 무엇을 근거로 예측하는지 명확히(ATR 도달확률 + 과거 분포). 점추정은 과신 유발 → **범위(P10/P50/P90)** 권장.
8. **LLM 비용·지연·환각** — 수백 종목 × 멀티에이전트 = 토큰 폭증. 티어링(통과분만 LLM) 필수. 뉴스 환각 방지 위해 **원문 주입 + 출처 강제**.
9. **거래소 캘린더/DST/반차/휴장** — 한·미 일정·서머타임 상이. 스케줄러가 캘린더 인지 못하면 오작동.
10. **데이터 정합성** — 분할/배당 조정, 통화, 동시호가, 상하한가, 거래정지 처리.
11. **추천 ↔ 폭주 알림의 연계** — 폭주 종목이 추천에 없을 수 있음. 우선순위/태그 정책 필요.
12. **유니버스 규모·API rate limit** — KR ~2500 + US ~5000+ 전수 스캔·모니터는 비현실적. 사전 시총·거래대금 필터로 축소.
13. **노션 API 한계** — rate limit, DB 스키마 사전설계, 매일 upsert 전략.
14. **반자동 책임 경계** — 투자권유 아님 면책, 추천가 ↔ 체결가 슬리피지를 "조건" 표현으로 흡수.

---

## 7. 제안 진행 순서 (Phase)

| Phase | 내용 | 산출물 | 선행 의존 |
|-------|------|--------|-----------|
| **0** | 데이터 소스 확정(KIS 발급, 뉴스원) + 유니버스 정의 | settings/markets/universe | 사용자 결정 #1,2 |
| **1** | 일봉 다운로드 + 지표/전략 이식(통화버그 수정) | downloader, indicators, strategies | Phase 0 |
| **2** | 지수 매크로 레짐 + 스크리너 + 섹터 진단 | regime, screener, sector | Phase 1 |
| **3** | 백테스트 라벨러 → 임계값 1차 검증(Walk-Forward) | backtest/labeler | Phase 2 |
| **4** | Claude 멀티 에이전트(뉴스/수급/밸류/종합) | agents/* | Phase 1, 뉴스원 |
| **5** | 종합추천 → Telegram 요약 + Notion 게시 | portfolio_agent, notion, telegram | Phase 2~4 |
| **6** | 실시간 개장 거래량 폭주 감지·알림 | realtime/volume_monitor | KIS 실시간 |
| **7** | 스케줄러 통합(US마감+1h / KR·US 개장) | pipeline/scheduler | 전부 |

---

## 8. 지금 사용자에게 필요한 결정 (Phase 0 차단요소)

1. **KIS Open API** 계좌·앱키를 발급할 수 있는가? (Stage 7 실시간의 사실상 유일한 무료 경로)
2. **뉴스 소스** 선호/예산? (국내·미국 각각)
3. **유니버스 범위** — 전 종목 vs 거래대금 상위 N vs 관심종목 리스트?
4. **노션 워크스페이스/DB** — 기존 페이지에 붙일지, 신규 DB 생성 권한 있는지?

---

*본 문서는 착수 전 합의용 설계안이다. 위 §6 차단요소(특히 #1 실시간, #2 뉴스원)와 §8 결정사항이 확정되면 Phase 0부터 구현을 시작한다. 모든 전략 임계값은 §6-6 백테스트로 재검증함을 전제로 한다.*
