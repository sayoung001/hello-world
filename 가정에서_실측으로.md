# 가정에서 실측으로

> **Implementation Runbook · US Equities**
> 손으로 정한 값 세 곳을 데이터가 정하도록 바꿨다.
> 결과 라벨링 루프 · 횡단면 모멘텀 · 적응형 임계 — 구현 · 실행법 · 출력 · 검증 결과.

| 상태 | |
|---|---|
| ① 라벨링 루프 | ✅ 완료 |
| ② 횡단면 모멘텀 | ✅ 완료 |
| ③ 적응형 임계 | ✅ 완료 |
| 모듈 검증 | 50개 import OK |

---

## 0. 한눈에

세 가지 모두 같은 문제를 푼다 — **지금까지 가정이던 것을 측정으로 바꾼다.**

| 항목 | 이전 (가정) | 이후 (실측) | 신규 파일 |
|---|---|---|---|
| **① 결과 라벨링** | 추천 결과를 기록하지 않음 — 잘 맞는지 알 수 없음 | 전 건에 삼중 배리어 결과 부착 → 점수별 실제 승률 산출 | `tracking/` 3개 |
| **② 횡단면 모멘텀** | 모멘텀 피처가 `ROC 12` 하나뿐 | 12-1 모멘텀 · 변동성조정 · 유니버스 백분위 순위 | `strategy/cross_sectional.py` |
| **③ 적응형 임계** | 고정 `RVOL ≥ 3.0` — 레짐 바뀌면 무너짐 | 실측 분포의 상위 1% 분위수를 컷으로 | `realtime/adaptive_thresholds.py` |

### 실행 순서

```bash
python -m stock_auto.pipeline.run_daily_batch --market US    # 추천 + 자동 기록
python -m stock_auto.pipeline.run_monitor --market US        # 알림 + 적응임계 + 기록
python -m stock_auto.pipeline.label_outcomes --out report.md # 라벨링 + 리포트
```

---

## 1. 결과 라벨링 루프

모든 개선의 전제. 이것이 없으면 가중치도 임계값도 **고칠 근거가 생기지 않는다.**

### 설계 결정 세 가지

| 결정 | 선택 | 이유 |
|---|---|---|
| **진입 시점** | 신호 다음 거래일 **시가** | 신호일 종가 진입은 실현 불가능한 look-ahead — 성과를 체계적으로 부풀린다 |
| **동시 도달** | 보수적으로 **SL 우선** | 일봉만으로는 장중 순서를 알 수 없다. 낙관 가정은 백테스트를 거짓말하게 만든다 |
| **기록 범위** | 실행 여부와 **무관하게 전 건** | "안 샀으니 기록 안 함"은 선택 편향을 만든다. 실행/보류/무시는 별도 컬럼으로만 |

### 삼중 배리어

각 신호에 상단(목표) · 하단(손절) · 수직(보유기간 상한) 배리어를 세우고 **먼저 닿는 것**으로 라벨링한다.
보유기간은 추천 `horizon`을 따른다 — 단타 3일 · 중단기 7일 · 스윙 20일.
목표·손절이 없으면 ATR 배수(2.0 / 1.2)로 대체한다.

> **이미 절반은 있었다**
> 기존 `calculate_strategy_levels`가 전략별 `tp_atr`/`sl_atr`로 목표·손절을 산출하고 있었다.
> 그 값을 그대로 배리어로 재사용했으므로 라벨링 기준이 **실제 추천에 쓴 기준과 정확히 일치**한다.

### 파일 구성

| 파일 | 역할 |
|---|---|
| `tracking/store.py` | 신호 적립. 판단(추천/확신도)뿐 아니라 **피처**(점수·레짐·모멘텀·ATR·RVOL)를 기록 시점에 함께 저장 — 나중에 캘리브레이션 학습이 가능하도록 |
| `tracking/labeler.py` | 삼중 배리어 라벨링. MAE(최대 역행)·MFE(최대 순행)·방향정확도 산출 |
| `tracking/report.py` | 성과 + 캘리브레이션 표 + 단조성 점검 + 학습 데이터셋 추출 |
| `pipeline/label_outcomes.py` | CLI. `--out` 파일 · `--notion` 게시 · `--telegram` 요약 · `--export-csv` 학습셋 |

### 출력 — 리포트

핵심 산출물은 **캘리브레이션 표**다. 점수 구간이 올라갈수록 실제 승률이 단조 증가해야 정상이며,
그렇지 않으면 가중치가 확률을 반영하지 못한다는 신호다. 아래는 합성 데이터로 생성한 실제 출력이다.

```
# 신호 성과 리포트

## 1. 요약

- 총 신호 160건 · 라벨 완료 160건 · 결과 대기 0건 · 미체결(갭) 0건
- 승률 62.5% · 평균수익 +1.81% · PF 2.20
- 평균 익절 +5.32% / 평균 손절 -4.04%
- 평균 MFE(순행) +7.18% / 평균 MAE(역행) -3.97%
- 청산 유형 — TP 100 · SL 60 · 타임아웃 0
- 방향 정확도 88.1% (MFE ≥ |MAE| 기준)

> 진단 — 방향 정확도는 높은데 SL 히트 비중이 큽니다. 신호가 아니라
  실행 시점/손절 폭의 문제일 가능성이 큽니다 → 메타라벨링 또는 동적 SL 검토.

## 2. 캘리브레이션

### Effective Score 구간별

| 구간    | 건수 | 승률 | 평균수익 |    PF | TP/SL/TO |
|---------|-----:|-----:|---------:|------:|----------|
| -99 ~ 2 |   17 |  41% |   +0.24% |  1.11 | 7/10/0   |
| 2 ~ 3   |   28 |  39% |   -0.41% |  0.82 | 11/17/0  |
| 3 ~ 4   |   24 |  54% |   +0.68% |  1.33 | 13/11/0  |
| 4 ~ 5   |   37 |  68% |   +2.28% |  2.55 | 25/12/0  |
| 5 ~ 6   |   40 |  78% |   +3.29% |  4.88 | 31/9/0   |
| 6+      |   14 |  93% |   +4.65% | 32.59 | 13/1/0   |

> 단조성 점검 — ⚠️ 승률이 단조 증가하지 않음 (41% → 39% 구간에서 역전)

## 3. 소스별 (배치 추천 vs 폭주 알림)

| 소스      | 건수 | 승률 | 평균수익 |   PF |
|-----------|-----:|-----:|---------:|-----:|
| 배치 추천 |  120 |  65% |   +2.08% | 2.50 |
| 폭주 알림 |   40 |  55% |   +0.99% | 1.53 |

> 폭주 알림의 승률이 곧 알림 정밀도입니다.
```

표에 담긴 두 가지가 특히 중요하다.
첫째, **진단 문구가 자동으로 붙는다** — 방향 정확도가 높은데 SL 히트가 많으면
"신호가 아니라 실행의 문제"라고 알려준다.
둘째, **폭주 알림 승률이 곧 알림 정밀도**이며 배치 추천과 나란히 비교된다.

### 검증 결과

| 케이스 | 입력 | 결과 | 판정 |
|---|---|---|---|
| TP 도달 | 진입 100, 목표 110, 3일차 고가 112 | `tp` · ret +10.00% · MFE +12% · MAE −1% · 2일 | ✅ |
| SL 도달 | 진입 100, 손절 95, 2일차 저가 94 | `sl` · ret −5.00% · MAE −6% · dir 0 | ✅ |
| 동시 도달 | 같은 날 고가 111 · 저가 94 | `sl` (보수적 우선) | ✅ |
| 타임아웃 | 중단기 · 8일 횡보 | `timeout` · label −1 · 7일 | ✅ |
| ATR 폴백 | 목표/손절 없음 · ATR 5.0 | TP=100+2×5=110 도달 → `tp` | ✅ |
| 데이터 부족 | 결과가 아직 안 나온 신호 | `nodata` (다음 실행에 재시도) | ✅ |

---

## 2. 횡단면 모멘텀

Gu · Kelly · Xiu(2020)가 지목한 세 축 — 모멘텀 · 유동성 · 변동성 — 중
유일하게 비어 있던 자리를 채운다.

기존 시스템은 유동성(Liquidity Score)과 변동성(ATR)은 갖췄지만 모멘텀은 `ROC 12` 하나뿐이었다.
추가한 것은 세 가지다.

- **12-1 모멘텀** — `t−252 → t−21` 수익률. 최근 1개월을 건너뛰는 이유는
  단기 반전 효과와 섞이지 않게 하기 위함 (Jegadeesh & Titman 1993).
- **변동성 조정 모멘텀** — 모멘텀 ÷ 연율화 변동성. 모멘텀 크래시 완화 (Barroso & Santa-Clara).
- **횡단면 백분위 순위** — 절대값 대신 유니버스 내 0~1 순위. 레짐이 바뀌어도 의미가 유지된다.

### 검증 출력

```
종목      mom_12_1   vol_ann   mom_rank  rank_vadj
STRONG      +0.070    +0.187     +1.000     +1.000
MID         -0.003    +0.212     +0.500     +0.500
WEAK        -0.101    +0.185     +0.250     +0.250
SHORT         None    +0.198     +0.750     +0.750   ← 이력 부족, 폴백 구간 적용

순위 단조성 (STRONG > MID > WEAK): True
이력부족 종목 폴백 랭크 부여     : True
```

이력이 252봉에 못 미치는 신규 상장 종목은 `mom_12_1`을 계산할 수 없다.
이때 가능한 가장 긴 구간으로 대체해 **랭킹에서 제외되지 않게** 했다 —
값이 없다고 빼버리면 신규 종목이 구조적으로 누락된다.

> **스크리너 연결**
> 유니버스 전체를 한 번에 계산해 상대 순위를 매긴 뒤 각 종목 행에 붙인다.
> 추가된 컬럼: `mom_rank` · `mom_12_1` · `mom_vol_adj`, 그리고 라벨링에 필요한 `atr` · `rvol`.
> 이 값들은 기록 시점에 저장되므로 **나중에 "모멘텀 순위가 실제로 승률을 예측했는가"를
> 리포트에서 바로 확인**할 수 있다.

---

## 3. 적응형 임계

고정 `RVOL ≥ 3.0`은 레짐이 바뀌면 무너진다 —
조용한 날엔 알림이 하나도 없고, 변동성 국면엔 전 종목이 동시에 뜬다.

관측 저장소에 쌓인 **실제 RVOL 분포**에서 상위 `q` 분위를 컷으로 쓴다.
"RVOL 3 이상"이 아니라 *"평소 이 시간대 대비 상위 1%"* 다.
RVOL은 관측된 누적거래량을 프로파일 평균으로 나눠 재구성한다.

### 검증 출력 — 12종목 × 60일 관측

```
적응 임계 (실측 분위수 기반)
  US 1분 윈도우: RVOL ≥ 3.81  (상위 1%, 표본 720)
  US 3분 윈도우: RVOL ≥ 3.86  (상위 1%, 표본 720)
  US 5분 윈도우: RVOL ≥ 3.92  (상위 1%, 표본 720)

고정 임계 대비
  1분: RVOL ≥ 3.00  →  3.81
  3분: RVOL ≥ 3.00  →  3.86
  5분: RVOL ≥ 3.00  →  3.92

표본 부족 폴백 : RVOL ≥ 3.0  (고정값 유지 = True)
타 임계 보존   : 가격이동 5.0 · 거래대금 2,000,000 · 체결강도 1.2 (변경 없음)
```

이 표본에서는 고정값 3.0이 **너무 느슨했다**는 뜻이다 — 실제 분포의 상위 1%는 3.8~3.9였다.
고정 임계로 운영했다면 상위 1%가 아니라 상위 3~4%가 알림으로 나갔을 것이고,
그만큼 오탐이 섞였을 것이다.

> **안전장치**
> 표본이 `min_samples`(기본 50) 미만이면 **고정 임계로 폴백**한다.
> 또 분위수가 아무리 낮게 나와도 `2.0` 아래로는 내려가지 않고 `12.0` 위로도 올라가지 않는다 —
> 초기 데이터 부족 구간에서 임계가 극단으로 튀는 것을 막는다.
> RVOL만 대체하고 **가격이동 · 거래대금 · 체결강도 임계는 그대로 보존**한다.

```bash
python -m stock_auto.pipeline.run_monitor --market US --quantile 0.99
python -m stock_auto.pipeline.run_monitor --market US --fixed-thresholds  # 끄기
```

---

## 4. 구현 중 발견한 버그

통합 테스트 출력을 읽다가 드러난 두 건. 둘 다 조용히 틀리는 종류였다.

### 버그 1 · 갭 통과 미처리

- **증상** — `exit_type=sl`인데 `ret=+86%`. 손절이 수익으로 기록됐다.
- **원인** — 진입가(다음날 시가)가 이미 손절선 아래에서 시작한 경우,
  코드가 그대로 "손절 도달"로 처리하면서 `(손절가 − 진입가)/진입가`가 *양수* 가 됐다.
- **영향** — 갭다운 종목의 손실이 수익으로 집계돼 성과 통계 전체가 오염된다.
  실전에서 흔히 발생하는 케이스라 방치하면 리포트를 믿을 수 없게 된다.
- **수정** — 진입가가 이미 배리어를 넘어섰으면 `skip`(미체결)으로 처리.
  조건부 진입 규칙("시가 −1% 눌림 & RVOL>1.5")을 쓰는 시스템에서는
  갭으로 손절선을 뚫고 시작하면 애초에 진입이 성립하지 않기 때문이다.
  성과 집계에서 제외하되 **건수는 리포트에 표시**해 숨기지 않는다.

### 버그 2 · RVOL 미기록

- **증상** — 기록된 신호의 `rvol` 컬럼이 비어 있었다.
- **원인** — `from_screen_row`가 `rsi_14`, `adx`, `mom_rank`는 매핑했지만 `rvol`을 빠뜨렸다.
- **영향** — 캘리브레이션 학습에서 거래량 피처가 통째로 빠진다.
  나중에 발견했다면 *이미 쌓인 데이터를 되살릴 수 없다* — 기록 누락은 소급 복구가 안 된다.
- **수정** — 매핑 추가. 검증 출력에서 값이 채워지는 것을 확인.

> **교훈**
> 두 버그 모두 **테스트가 통과했는데도** 출력 숫자를 눈으로 읽다가 찾았다.
> "exit_type이 sl인데 수익이 +86%"는 어떤 assert도 잡지 못했다.
> 자동 검증은 *알고 있는 실패* 만 잡는다.

---

## 5. 운영 캘린더

| 시점 | 명령 | 산출 |
|---|---|---|
| **US 마감 +1h** (자동) | `run_daily_batch --market US` | 추천 → Notion 게시 + **신호 자동 적립** |
| **US 개장** (자동) | `run_monitor --market US` | 폭주 알림(적응 임계) → Telegram + **알림 적립** + 관측 적립 |
| **주 1회** | `label_outcomes --out report.md` | 결과 라벨링 + 성과·캘리브레이션 리포트 |
| **월 1회** | `label_outcomes --export-csv cal.csv` | 캘리브레이션 학습셋 (TP=1 / SL=0, 피처 13개) |

데몬(`run_daemon`)을 띄워 두면 앞의 둘은 자동으로 돌고,
관측이 쌓일수록 프로파일과 적응 임계가 함께 정밀해진다.
라벨링만 주 1회 수동으로 돌리면 된다.

---

## 6. 다음 단계

세 기능은 모두 **데이터를 만드는 장치**다. 그 자체로 수익을 내지는 않는다.
쌓인 데이터가 임계 200건을 넘으면 다음 층으로 넘어간다.

| 표본 | 가능해지는 것 |
|---|---|
| **~50건** | 알림 정밀도 기준선 확인. 임계값이 너무 느슨한지 빡빡한지 판단 |
| **~100건** | 점수 구간별 승률의 **단조성** 확인 — 가중치가 변별력을 가지는지 |
| **~200건** | **로지스틱 캘리브레이션 학습 가능** — 점수 → `P(승)` 변환. 여기서부터 확률 기반 사이징(Kelly)이 성립한다 |
| **~500건** | 메타라벨링 2차 모델 — "이 신호를 실행할 것인가" |

> **지금 해야 할 유일한 일**
> 데몬을 켜 두고 **주 1회 라벨링을 돌리는 것**.
> 하루 5건 추천이면 40거래일, 약 2개월 뒤 캘리브레이션이 가능해진다.
> 그 전까지 가중치나 임계값을 손으로 조정하는 것은 근거 없는 추측이며,
> 오히려 쌓이는 데이터의 일관성을 깨뜨린다 —
> *당분간은 건드리지 말고 모으는 것이 최선의 행동이다.*

---

*구현 검증은 합성 데이터 기반이며 전략의 수익성을 보장하지 않는다.
리포트 예시 수치는 테스트용 합성 신호에서 생성된 것으로 실제 성과가 아니다.
본 문서는 시스템 운영 문서이며 투자 자문이 아니다.*
