# 미국장 실전 투입 플랜

작성 2026-09-07 · 대상 `sayoung001/hello-world` @ `claude/trusting-heisenberg-387xeq`
**갱신 2026-09-07 (2차)** — Phase 0의 6개 항목을 구현 완료. 아래 표에 반영.

---

## 0. 결론 먼저

지금 이 시스템은 **추천 생성기이지 매매 시스템이 아니다.** 코드 전체에 주문 관련 라인은
`kis_feed.py`의 주석 한 줄("시세 조회만 사용, 주문 권한 불필요")뿐이다.

그런데 **지금 채워야 할 것은 주문 코드가 아니다.** 순서가 이렇다.

| 층 | 지금 상태 | 안 채우고 넘어가면 |
|---|---|---|
| ① 데이터 | **유니버스 지수 구성종목(~514) 연결 완료** · 소스 단일화 완료 | 틀린 데이터로 표본이 쌓임. 소급 복구 불가 |
| ② 검증 | 알파 존재 여부를 **아직 모름**(합성 데이터 기준선만 있음) | 근거 없이 돈이 들어감 |
| ③ 실행 | 주문·시차·환율·세금 전무 | 신호는 맞는데 계좌는 마이너스 |

②를 건너뛰고 ③을 만들면 "잘 돌아가는데 돈을 잃는 시스템"이 된다.
①을 건너뛰고 ②를 하면 그 검증 결과가 거짓이다. **①→②→③ 순서를 지켜야 한다.**

---

## 1. 문서 2편이 이미 잡은 것

`금융분석_관점_점검_및_보완필요.md`의 우선순위 표는 판단이 정확하다. 요약만 옮긴다.

- ~~실적 발표일 필터 없음~~ → **구현 완료** (`data/earnings.py`, D-3 ~ D+1 차단)
- 거래비용 없음 → 왕복 30bp만 넣어도 중단기 7일 승률 40.8%가 손익분기 40.0%에 딱 걸림
- ~~유니버스 20종목이면 캘리브레이션까지 24개월~~ → **지수 구성종목 연결로 ~1.6개월**
- 보유 종목 파일 없음 → 청산 신호·실손익이 여전히 막힘 (트리아지는 별도 경로로 해결)
- 단타 3일 × 목표 2 ATR 조합이 물리적으로 안 맞음(41% 타임아웃)
- ~~유동성 사전필터 $2M vs 점수 중심 $50M~~ → **$20M로 상향 + 설정 분리 완료**
- 섹터 집중도 제약 없음 / 벤치마크 대비 초과수익 없음 / 사이징 없음

---

## 2. 코드를 직접 읽고 찾은 것 (1차) + 구현 결과

### 2-1. `[해결]` 데몬이 `fdr_universe`를 호출하지 않았다

`run_daemon` / `run_monitor` / `build_profiles` 전부 `load_universe`(샘플 20종목)를 썼고,
`fdr_universe`를 부르는 곳은 CLI 한 곳뿐이었다. 데몬을 켜면 문서가 경고한
**24개월 경로에 그대로 올라타는** 상태였다.

**조치** — 공통 진입점 `resolve_universe(market)`를 만들어 세 곳을 모두 교체했다.
`STOCK_UNIVERSE=sample` 환경변수로 디버깅 시 샘플 강제 전환이 가능하다.
배치 로그에 `유니버스 N=___`을 찍고, **100종목 미만이면 경고**한다.

### 2-2. `[해결]` `fdr_universe`가 상위 종목을 뽑지 않고 알파벳 앞 300개를 잘랐다

`fdr.StockListing("NASDAQ").head(300)` — 정렬이 한 줄도 없었고 NYSE·AMEX가 빠졌다.
유니버스를 300으로 키우는 순간 "A로 시작하는 나스닥 마이크로캡 300개"를 스캔하게 된다.

**조치** — 접근 자체를 바꿨다. **거래대금 순위 대신 지수 구성종목**을 쓴다.

```
US 유니버스 = S&P500 구성종목 ∪ 나스닥100 차집합  ≈ 514종목
```

이유: (a) 유동성이 지수 편입 요건으로 이미 보장되고 (b) 거래소를 가리지 않으며
(c) 리밸런싱을 지수 제공자가 대신해 준다. 6,000종목의 20일 거래대금을 매일 계산하는
것보다 훨씬 싸고 결과도 낫다.

- `NASDAQ100_EXTRA`는 S&P500에 없는 NDX 종목(주로 미국 외 법인)만 담은 보충 목록이다.
  **지수 리밸런싱(연 1회)마다 낡으므로 분기 확인이 필요하다.**
- 조회 결과는 `data/universe/universe_US_YYYY-MM-DD.csv`로 **날짜별 스냅샷** 저장.
  나중에 "그때 그 신호가 어떤 유니버스에서 나왔는지" 재현할 수 있다.
- 폴백 체계: 라이브 → 마지막 스냅샷 → 샘플. **각 단계마다 경고를 찍는다.**
  50종목 미만은 스냅샷으로 저장하지도, 읽지도 않는다(반쪽 목록이 굳는 것을 막는다).

> ⚠️ **네트워크 정책 주의** — FinanceDataReader의 S&P500 구성종목은
> **Wikipedia(`en.wikipedia.org`)** 에서, 일봉은 **Yahoo(`query2.finance.yahoo.com`)** 에서
> 온다. SSH VM의 아웃바운드 정책에서 **두 도메인을 모두 열어야** 한다.
> 하나만 열면 "지수는 받아지는데 유니버스가 20종목"인 상태가 된다.

### 2-3. `[해결]` 데이터 소스 혼합 + 수정주가 정합성

FDR과 yfinance를 섞어 쓰면서 종목별로 어느 소스에서 왔는지 기록이 없었고,
액면분할 시 캐시 앞부분(옛 조정계수)과 뒷부분(새 계수)이 이어 붙어
**시계열 중간에 인위적 가격 점프**가 생길 수 있었다.

**조치**
- US는 **yfinance(`auto_adjust=True`) 우선**, FDR은 폴백으로 강등해 정책을 단일화.
- 캐시에 소스를 기록(`.meta.json`). 소스가 바뀌면 캐시를 폐기하고 새로 받는다.
- **캐시 병합 시 겹치는 날짜의 종가를 비교**해 0.5% 넘게 다르면 조정계수 변경으로 보고
  **캐시 전량을 폐기하고 재다운로드**한다. 발생 시 로그에 남는다.

### 2-4. `[남음]` 라벨러의 진입 가정과 추천의 진입 규칙이 다르다

라벨러는 **다음 거래일 시가** 진입을 가정하는데, 추천에는 `entry_rule`(조건부 진입)과
`entry_zone`(가격 구간)이 있고 라벨러는 이를 읽지 않는다.
"시가 −1% 눌림에서 산다"는 추천을 "시가에 샀다면"으로 채점하고 있다.

**아직 결정하지 않았다.** 2-7의 예약주문 설계와 함께 정해야 한다.
시차 때문에 어차피 MOO/시가 진입이 현실적이므로 **(a) 시가 진입으로 통일**이 유력하다.

### 2-5. `[해결]` 게이트가 표본 수집을 통째로 끊었다

매크로/섹터 게이트에 막힌 신호는 `candidates`에서 빠지고 **기록조차 되지 않았다.**
하락장 구간 데이터가 통째로 비어 "게이트가 실제로 효과가 있었는가"를 영원히 검증할 수 없었다.

**조치**
- 스크리너가 `raw_buy`(규칙엔진 원신호)와 `final_buy`(실행 가능 신호)를 분리하고
  `gated_by` 컬럼에 차단 사유(`macro` / `sector` / `earnings`)를 남긴다.
- `ScreenResult.gated`에 차단분을 담아 배치가 **동일하게 기록·라벨링**한다.
- 리포트에 **5장 「게이트 실효성 (통과분 vs 차단분)」** 을 추가했다.
  차단분 성과가 통과분보다 좋으면 "게이트가 수익 기회를 버리고 있다"고 경고한다.
- 차단분은 LLM 추천이 없어 `horizon`이 비는데, 그러면 라벨러가 5일로 대체해
  통과분(단타 3일)과 보유기간이 달라진다. → **규칙엔진의 `styles`를 보유기간으로** 쓴다.
  (이 비교 자체가 서로 다른 조건의 비교가 되는 것을 막는다.)

### 2-6. `[남음]` 매일 라벨링이 전 종목 재다운로드

`load_or_download(..., use_cache=False)`로 미라벨 종목 전부를 매일 다시 받는다.
유니버스 500종목에서 미라벨이 누적되면 yfinance 레이트리밋에 걸린다.
2-3의 신선도 판정이 들어왔으므로 이제 캐시를 켜도 안전하다. 다음 작업으로 남긴다.

### 2-7. `[남음]` 한국에서 미국 주식을 하는 데서 오는 것들

코드에 `환율`·`fx`·`세금` 문자열이 **0건**이다.

- **시차** — 배치는 KST 06:00/07:00에 나오는데 라벨러가 가정하는 진입 시점은
  KST 22:30/23:30이다. 매일 밤 수동 주문은 몇 주도 못 간다.
  → 실행 설계는 처음부터 **예약주문(MOO/지정가 GTD) 전제**로.
- **환율** — 환전 스프레드가 리포트에 안 들어간다. 거래비용 30bp에 환전 비용을 더하면
  **중단기 7일도 손익분기 아래로 내려갈 수 있다.**
- **세금** — 미국주식 양도소득세(분리과세). 회전율이 높을수록 세후가 깎인다.
  세율·공제액은 매년 바뀌니 실행 직전 최신 안내로 확인할 것.

### 2-8. `[신규·해결]` KIS 실시간 등록 한도를 넘는 구독

2-1을 고쳐 유니버스가 500종목이 되자 **새 문제가 드러났다.**
KIS Open API는 접속(approval_key)당 실시간 등록 건수가 제한(정규 40건 내외)되는데,
`kis_feed.stream()`은 심볼 수만큼 구독 메시지를 보내고 **응답을 확인하지 않는다.**
500개를 보내면 한도 초과분이 조용히 빠지고, 어느 종목이 빠졌는지 알 수 없다.

**조치** — `realtime/watchlist.py`를 만들어 감시 대상을 우선순위로 추린다.

```
1. 보유 종목        — 청산 판단 필요 (positions.csv 생기면 자동 반영)
2. 직전 배치 후보   — 오늘 살지도 모르는 종목 (최근 5일, Effective 순)
3. 남는 자리에 유니버스 상위
```

한도는 `KIS_MAX_SUBSCRIPTIONS` 환경변수로 조정한다(기본 40).
"온 시장 감시"가 아니라 **"오늘 관심 있는 종목만 감시"** 로 방향을 바꾼 것이며,
사용자 정책("추천 위주로만 확인")과도 맞는다.

---

## 3. 실행 플랜

### Phase 0 — 실데이터를 붙이기 전 (코드만)

| # | 작업 | 상태 |
|---|---|---|
| 0-1 | 유니버스 제대로 만들기 (지수 구성종목 + 날짜별 스냅샷) | ✅ 완료 |
| 0-2 | 데몬·모니터·프로파일러를 실유니버스에 연결 | ✅ 완료 |
| 0-3 | 데이터 소스 단일화 + 출처 기록 | ✅ 완료 |
| 0-4 | 수정주가 점프 탐지 (겹치는 날짜 종가 0.5% 괴리 → 캐시 폐기) | ✅ 완료 |
| 0-5 | 사전필터 상향($20M) + 일봉/폭주 하한 설정 분리 | ✅ 완료 |
| 0-6 | 게이트 차단분도 기록 (`gated_by`) | ✅ 완료 |
| 0-7 | 진입 규칙 통일 (시가 진입 vs entry_zone) | ⬜ 남음 (2-4) |
| 0-8 | 거래비용 파라미터 (`cost_bps` 기본 30) | ⬜ 남음 |
| 0-9 | 실적 발표일 게이트 | ✅ 완료 (원래 Phase 2였으나 앞당김) |
| 0-10 | 트리아지 입력 경로 | ✅ 완료 (원래 Phase 2였으나 앞당김) |
| 0-11 | KIS 실시간 등록 한도 대응 | ✅ 완료 (2-8) |

**Phase 0 완료 판정** — SSH 서버에서 아래를 돌려 전부 통과.

```bash
python -m stock_auto.tools.check_pipeline
```

기대 출력: `지수 3종 수집 ✅ · 유니버스 N≥500 ✅ · 일봉 수집 실패 0 ·
섹터 12개 진단 ✅ · 실적 일정 확인 ✅ · 스크리너 실행 ✅`

> 이 환경(개발 컨테이너)에서는 아웃바운드가 막혀 있어 **지수·유니버스 수집이 전부
> 실패로 나온다.** 자가점검 도구는 그 실패를 정확히 잡아내며, 실검증은
> **네트워크가 열린 SSH VM에서** 해야 한다.

### Phase 1 — 알파가 있는지 확인 (Go/No-Go)

여기서 **계속할지 말지를 정한다.**

| # | 작업 | 산출 |
|---|---|---|
| 1-1 | 과거 2년 백필 라벨링 | 유니버스 전 종목 × 2년 → `Final_Buy` 전건 → 삼중 배리어. `source="backfill"`로 분리 집계 |
| 1-2 | **핵심 지표 측정** | SL 히트 건 중 `direction_correct == 1` 비율 |
| 1-3 | 벤치마크 병기 | 같은 기간·보유일수 SPY 수익률, 초과수익, 정보비율 |
| 1-4 | horizon별 재조정 | 3/7/20일 각각의 TP·SL 배수 그리드 |
| 1-5 | 발동률 실측 | MeanReversion·GapMomentum이 실데이터에서도 0%인지 |

**Go / No-Go 기준 (1-2)**

| 결과 | 해석 | 다음 |
|---|---|---|
| **50% 이상** | 방향은 맞는데 손절 폭이 문제 | 동적 SL·메타라벨링 → Phase 2 |
| **20~50%** | 약한 신호. 비용 차감 후 남는지 확인 필요 | 1-4로 배리어 재설계 후 재측정 |
| **10% 근처** | 합성 기준선(7.0%)과 같음 = **신호에 방향 정보가 없다** | **중단.** 전략 재설계 |

> 이 한 숫자에 프로젝트의 존폐가 걸려 있다. 실데이터를 받는 즉시 가장 먼저 재라.
> 이 값이 나오기 전에 가중치·임계값을 손대지 말 것.

### Phase 2 — 실전 안전장치 (Phase 1 통과 시에만)

| # | 작업 | 상태 |
|---|---|---|
| 2-1 | **`data/positions.csv`** — 보유·진입가·수량·환율 | ⬜ 남음 (가장 큰 잔여 항목) |
| 2-2 | 실적 캘린더 D-3 차단 | ✅ 완료 |
| 2-3 | 리스크 기반 사이징 | ⬜ 남음 |
| 2-4 | 섹터 집중도 (상위 15 중 섹터당 최대 3) | ⬜ 남음 |
| 2-5 | 트리아지 입력 | ✅ 완료 |
| 2-6 | 휴장일·반장 캘린더 (`scheduler.py` 연결) | ⬜ 남음 |

### Phase 3 — 실행 층 (페이퍼 먼저)

| # | 작업 | 내용 |
|---|---|---|
| 3-1 | 브로커 결정 | KIS 해외주식 주문 API. **모의투자(`KIS_ENV=paper`)에서 최소 4주** |
| 3-2 | 예약주문 설계 | MOO 또는 개장 직후 지정가 GTD. 손절은 브로커 GTC로, 이중 손절 금지 |
| 3-3 | 환율 회계 | 진입·청산 환율을 `positions.csv`에. 리포트에 USD/KRW 두 줄 |
| 3-4 | 킬 스위치 | 일일 최대손실·연속손절·MDD 임계 도달 시 신규 진입 중단 |
| 3-5 | 페이퍼 → 실전 기준 | 모의 4주 + 체결가 괴리 왕복 50bp 이내 + 킬스위치 1회 실동작 |

> **자동 주문은 마지막이다.** Phase 2까지 끝내고 반자동으로 최소 한 달 돌린 뒤
> 그때도 시스템이 사람보다 나으면 자동화한다.
> **`executed` 트리아지 표본이 그 판단의 유일한 근거다** — 이제 입력 경로가 생겼으니
> 오늘부터 매일 입력해야 한다. 소급이 안 된다.

---

## 4. 다음에 할 3가지

1. **SSH VM에서 `check_pipeline` 실행** — 지수 3종·유니버스 500·실적 캘린더가
   실제로 받아지는지 확인. Wikipedia/Yahoo 두 도메인 아웃바운드 허용 필요.
2. **`data/positions.csv`** (Phase 2-1) — 청산 신호를 보유분에 연결하고 실손익을 집계한다.
   지금은 "안 산 종목의 매도 신호"까지 알려주고, "산 종목이 유니버스에서 빠지면
   매도 신호를 영영 못 받는다."
3. **거래비용 + 진입 규칙 통일** (0-7, 0-8) — Phase 1의 Go/No-Go 판정이
   비용 반영 여부에 따라 뒤집히므로, 측정 **전에** 넣어야 한다.

---

*본 문서는 시스템 개발·운영 계획이며 투자 자문이 아니다. Phase 1의 Go/No-Go 지표가
확인되기 전의 모든 성과 수치는 합성 데이터 기준선이며 실제 수익성을 뜻하지 않는다.
세율·수수료·실시간 시세 정책은 변동되므로 실행 직전 최신 고지로 재확인할 것.*
