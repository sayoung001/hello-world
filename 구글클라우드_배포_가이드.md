# 구글 클라우드에 올리기 — 처음부터 끝까지

작성 2026-09-08 · **zip 수동 업로드 방식** (git·VS Code 없이)

이 문서는 서버를 처음 다뤄 보는 사람을 기준으로 씁니다.
**명령어는 그대로 복사해서 붙여 넣으면 됩니다.**

---

## 0. 전체 그림 — 무엇을 하는 건지

```
내 컴퓨터                 구글 클라우드 VM (24시간 켜져 있는 리눅스 컴퓨터)
┌──────────┐             ┌────────────────────────────────────┐
│ 코드 zip  │  ─업로드→   │  코드 압축 풀기                      │
└──────────┘             │  파이썬·라이브러리 설치               │
                         │  .env 에 비밀키 입력                  │
                         │  자동 실행 등록 (systemd)             │
                         └────────────────┬───────────────────┘
                                          │ 매일 자동 실행
                                          ▼
                              내 폰 텔레그램으로 추천 도착
```

**소요 시간** 처음 한 번 40분~1시간(대부분 첫날 일봉 다운로드 대기). 두 번째부터는 5분.

**사양** `e2-small`(2 vCPU / 2GB)이면 충분합니다. 실측 최대 메모리가 약 200MB로
2GB의 12%밖에 안 씁니다. 근거는 1-2 아래 표에 있습니다.

**비용** `e2-small` 기준 월 1~2만 원대. 아래 1-2에서 사양을 고릅니다.
정확한 금액은 [구글 클라우드 가격 계산기](https://cloud.google.com/products/calculator)에서
확인하세요(요금은 수시로 바뀝니다).

> **미리 알아둘 것 하나** — 이 시스템은 **주문을 넣지 않습니다.** 추천을 텔레그램으로
> 보내주는 것까지가 전부이고, 사고 파는 것은 사람이 직접 합니다.

---

## 1. 구글 클라우드에 컴퓨터(VM) 만들기

### 1-1. 프로젝트 준비

1. https://console.cloud.google.com 접속 → 구글 계정 로그인
2. 상단 왼쪽 **프로젝트 선택** → **새 프로젝트** → 이름 아무거나(예: `stock-auto`) → 만들기
3. 결제 계정이 없으면 안내에 따라 등록 (신규 가입 시 무료 크레딧이 있습니다)

### 1-2. VM 만들기

1. 왼쪽 메뉴(☰) → **Compute Engine** → **VM 인스턴스**
   - 처음이면 "Compute Engine API 사용 설정" 버튼이 나옵니다. 눌러서 켜고 1~2분 기다립니다.
2. **인스턴스 만들기** 클릭. 아래대로만 고르면 됩니다.

| 항목 | 고를 값 | 이유 |
|---|---|---|
| 이름 | `stock-auto` | 아무거나 |
| 리전 | `us-east1` 또는 `us-central1` | 미국 시장 데이터가 가까워 빠릅니다 |
| 머신 유형 | **e2-small** (2 vCPU / 2GB) | 실측상 여유가 큽니다(아래 표 참고) |
| 부팅 디스크 | **Ubuntu 22.04 LTS**, 크기 **10~20GB** | 데이터는 20MB 정도라 10GB로도 충분합니다 |
| 방화벽 | 체크 안 함 | 외부에서 접속할 웹서버가 아닙니다 |

3. **만들기** → 목록에 초록 체크가 뜰 때까지 1분 기다립니다.

### e2-small로 충분한가 — 실측값

500종목 유니버스 기준으로 직접 재본 값입니다.

| 항목 | 실측 | e2-small(2 vCPU / 2GB) 대비 |
|---|---:|---|
| 데몬 상주 메모리 | 148 MB | 7% |
| 배치 실행 중 최대 메모리 | 약 200 MB | **12%** |
| 규칙 스캔 500종목 | 23초 (4코어 기준) | 하루 1회, 여유 |
| 일봉 캐시 디스크 | 20 MB | 10GB의 0.2% |

**메모리는 병목이 아닙니다.** 실제로 오래 걸리는 건 **첫날 일봉 500종목 다운로드**(10~20분)인데,
이건 네트워크 대기 시간이라 CPU·메모리와 무관합니다.

다만 2GB 머신에는 **스왑을 만들어 두길 권합니다.** 평소엔 쓰이지 않지만, 스왑이 아예
없으면 예기치 못한 급증에서 리눅스가 프로세스를 그냥 죽입니다(OOM Killer).
스왑이 있으면 잠깐 느려질 뿐 죽지 않습니다. 명령 한 줄입니다 — 아래 3-3.

> **머신 유형 바꾸는 법** — 나중에 필요하면 VM을 중지하고 머신 유형만 바꿀 수 있습니다.
> 데이터는 그대로 유지됩니다. 다만 위 실측대로면 바꿀 일이 없을 겁니다.

### 1-3. 터미널 열기 (SSH)

VM 목록에서 `stock-auto` 줄의 오른쪽 **SSH** 버튼 클릭 → 검은 창이 새로 뜹니다.
**이 창이 "서버 안에서 명령을 치는 곳"입니다.** 앞으로 나오는 명령어는 전부 여기에 붙여 넣습니다.

> 붙여 넣기는 `Ctrl+V`가 안 될 수 있습니다. **마우스 오른쪽 클릭 → 붙여넣기**를 쓰세요.

---

## 2. 코드 zip 만들어 올리기

### 2-1. 내 컴퓨터에서 zip 만들기

프로젝트 폴더 전체를 압축합니다. 다만 **아래 항목은 빼는 것이 좋습니다** (용량만 크고 쓸모 없음).

| 빼야 할 것 | 이유 |
|---|---|
| `.git/` | 버전 기록. 서버에 필요 없고 매우 큼 |
| `__pycache__/`, `*.pyc` | 파이썬 임시 파일 |
| `data/` | 캐시. 서버가 알아서 새로 만듭니다 |
| `.venv/` | 가상환경. 서버에서 새로 만듭니다 |
| **`.env`** | **비밀키! 절대 zip에 넣지 마세요.** 서버에서 직접 입력합니다 |

**윈도우**: 파일 탐색기에서 위 폴더들을 지운 사본을 만든 뒤,
폴더 우클릭 → **압축(ZIP) 파일로 만들기** → `stock_auto.zip`

**맥**: 폴더 우클릭 → **"…" 압축**

> zip 안이 `stock_auto.zip/hello-world/stock_auto/...` 처럼 폴더가 한 겹 더 있어도 괜찮습니다.
> 아래 3-1에서 처리합니다.

### 2-2. 서버로 업로드

SSH 창 **오른쪽 위 톱니바퀴(⚙️) 또는 "파일 업로드"** 버튼을 누르고 `stock_auto.zip`을 고릅니다.
업로드가 끝나면 파일은 `/home/사용자이름/stock_auto.zip` 에 있습니다.

확인:

```bash
ls -lh ~/*.zip
```

`stock_auto.zip` 이 보이면 성공입니다.

---

## 3. 서버에 설치하기

### 3-1. 압축 풀기

```bash
cd ~
unzip -q stock_auto.zip -d unzipped
ls unzipped
```

- `stock_auto` `deploy` `requirements.txt` … 가 **바로** 보이면:
  ```bash
  mv unzipped ~/app
  ```
- 폴더 이름 **하나만** 보이면(예: `hello-world`) 그 안이 진짜입니다:
  ```bash
  mv unzipped/* ~/app
  rmdir unzipped
  ```

확인 — 아래 두 파일이 모두 보여야 합니다.

```bash
cd ~/app && ls requirements.txt deploy/setup_vm.sh
```

### 3-2. 설치 스크립트 실행

```bash
cd ~/app
bash deploy/setup_vm.sh
```

파이썬·라이브러리를 설치하고 `.env` 틀을 만듭니다. **3~5분** 걸립니다.
중간에 `[Y/n]` 을 물으면 그냥 **Enter**.

### 3-3. 스왑 만들기 (2GB VM이면 권장, 1분)

```bash
bash deploy/add_swap.sh
```

2GB 스왑을 만들고 재부팅 후에도 유지되게 등록합니다.
`vm.swappiness=10` 으로 잡아서 **평소에는 쓰이지 않고** 진짜 급할 때만 쓰입니다.

설치 스크립트가 메모리 2GB 이하 + 스왑 없음을 감지하면 이 명령을 안내해 줍니다.
확인:

```bash
free -h        # Swap 줄에 2.0Gi 가 보이면 성공
```

---

## 4. 비밀키 입력하기 (`.env`)

```bash
cd ~/app
nano .env
```

`nano`는 서버용 메모장입니다. 화살표로 이동해 `=` 뒤에 값을 붙여 넣습니다.

```
KIS_APP_KEY=여기에붙여넣기
KIS_APP_SECRET=여기에붙여넣기
ANTHROPIC_API_KEY=여기에붙여넣기
TELEGRAM_BOT_TOKEN=여기에붙여넣기
TELEGRAM_CHAT_ID=여기에붙여넣기
```

**저장하고 나오기**: `Ctrl+O` → `Enter` → `Ctrl+X`

### 각 키를 어디서 얻는지

| 키 | 없으면 어떻게 되나 | 얻는 곳 |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` | **추천을 못 받습니다** (필수에 가까움) | 아래 4-1 |
| `ANTHROPIC_API_KEY` | 규칙 스캔만 돌고 AI 분석·진입조건이 빠집니다 | https://console.anthropic.com |
| `KIS_APP_KEY` / `KIS_APP_SECRET` | 장중 실시간 폭주 알림이 빠집니다(일일 추천은 정상) | 한국투자증권 KIS Open API |
| `NOTION_TOKEN` 등 | 노션 아카이브만 빠집니다. **텔레그램으로는 그대로 옵니다** | https://www.notion.so/my-integrations |

### 4-1. 텔레그램 봇 만들기 (5분)

1. 텔레그램 앱에서 **@BotFather** 검색 → 대화 시작
2. `/newbot` 입력 → 봇 이름 → 봇 아이디(끝이 `bot` 이어야 함) 입력
3. `Use this token to access the HTTP API:` 아래 긴 문자열 → **이게 `TELEGRAM_BOT_TOKEN`**
4. 방금 만든 내 봇을 검색해 대화창을 열고 **아무 말이나 한 번 보냅니다** (예: `안녕`)
   - ⚠️ 이 단계를 빼먹으면 봇이 나에게 메시지를 못 보냅니다.
5. 아래 주소를 브라우저 주소창에 붙여 넣습니다(`토큰` 자리에 3번 값):
   ```
   https://api.telegram.org/bot토큰/getUpdates
   ```
6. 나오는 글자 중 `"chat":{"id":123456789` 의 숫자 → **이게 `TELEGRAM_CHAT_ID`**

### 4-2. 잘 들어갔는지 확인

```bash
cd ~/app
source .venv/bin/activate
python -m stock_auto.tools.preflight --live
```

- `✅ TELEGRAM_BOT_TOKEN: abcd…12` 처럼 나오면 인식된 것입니다.
- `⚠️ (미설정)` 이면 `.env`에 값이 안 들어갔거나 저장이 안 된 것입니다.
- `→ 데이터 루트 /home/…/app/data` 줄이 데이터가 쌓일 위치입니다.

> **`source .venv/bin/activate`** 는 "이 프로젝트 전용 파이썬을 쓰겠다"는 뜻입니다.
> **SSH 창을 새로 열 때마다 다시 쳐야 합니다.** 안 치면 `ModuleNotFoundError` 가 납니다.

---

## 5. 네트워크 열어주기 (중요 — 이걸 빼먹으면 20종목만 봅니다)

이 시스템은 바깥 사이트 세 곳에서 데이터를 받습니다.

| 주소 | 무엇을 받나 | 막히면 |
|---|---|---|
| `query2.finance.yahoo.com` | 일봉·지수(S&P500·나스닥·QQQ) | 아무것도 못 받음 |
| `en.wikipedia.org` | **S&P500 구성종목 목록** | **유니버스가 20종목으로 줄어듦** |
| `api.telegram.org` | 알림 전송 | 추천이 안 옴 |

기본 설정(외부 IP 있음)이면 셋 다 열려 있습니다. 회사 정책 등으로 막았다면 풀어야 합니다.
확인:

```bash
cd ~/app && source .venv/bin/activate
python -m stock_auto.tools.check_pipeline --quick
```

`✅ 유니버스 규모 — N=514종목` 처럼 나오면 정상입니다.
`❌ N=20 — 샘플 폴백으로 보입니다` 가 나오면 위키피디아가 막힌 것입니다.

---

## 6. 손으로 한 번 돌려보기

```bash
cd ~/app && source .venv/bin/activate
python -m stock_auto.tools.check_pipeline
```

7단계(지수 → 레짐 → 유니버스 → 일봉 → 섹터 → 실적 → 추천)를 점검합니다.
**처음에는 일봉 500종목을 받느라 10~20분** 걸립니다. 두 번째부터는 캐시가 있어 빠릅니다.

전부 ✅ 면 실제 배치를 한 번 돌립니다.

```bash
python -m stock_auto.pipeline.run_daily_batch --market US
```

**끝나면 텔레그램에 추천이 도착합니다.** 도착했다면 설치는 성공입니다.

> 처음이라 부담되면 `--sample-universe` 를 붙여 20종목으로 빠르게 시험해 볼 수 있습니다.
> 다만 **실제 운영에서는 절대 쓰지 마세요** — 20종목이면 데이터가 모이는 데 2년 걸립니다.

---

## 7. 자동 실행 등록하기

```bash
cd ~/app
bash deploy/install_service.sh
```

이걸로 끝입니다. 이제 **서버가 재부팅돼도 알아서 다시 뜨고**, 매일 자동으로 돕니다.

| 시각 | 하는 일 |
|---|---|
| 미국 마감 +1시간 (한국시간 **오전 6~7시**) | 결과 라벨링 → 데이터 갱신 → 섹터·실적 점검 → **추천 텔레그램 발송** |
| 미국 개장 (한국시간 **밤 10:30~11:30**) | 실시간 거래량 폭주 감시 시작 |

> 한국시간이 한 시간씩 왔다갔다 하는 건 미국 서머타임 때문입니다. 자동으로 맞춰집니다.

### 자주 쓰는 명령어

```bash
sudo systemctl status stock-auto      # 잘 돌고 있나 확인 (초록 active면 정상)
tail -f ~/app/logs/daemon.log         # 실시간 로그 보기 (Ctrl+C로 나오기)
sudo systemctl restart stock-auto     # 다시 시작
sudo systemctl stop stock-auto        # 멈추기
```

---

## 8. 매일 해야 할 것 (5분)

추천을 받으면 **샀는지 안 샀는지 반드시 입력하세요.**

```bash
cd ~/app && source .venv/bin/activate
python -m stock_auto.pipeline.triage
#  [y] 샀다  [n] 안 샀다  [s] 보류  [q] 종료
```

**안 산 것도 기록해야 합니다.** 그래야 "내 판단이 도움이 됐나"를 나중에 알 수 있고,
**이 기록은 나중에 소급해서 채울 수 없습니다.**

폰으로 목록만 받고 싶으면:

```bash
python -m stock_auto.pipeline.triage --telegram
```

주 1회 성과 확인:

```bash
python -m stock_auto.pipeline.label_outcomes --out report.md
```

자세한 읽는 법은 `추천_읽는_법과_확인사항.md` 를 보세요.

---

## 9. 코드를 새로 올릴 때 (두 번째부터)

새 zip을 업로드한 뒤:

```bash
cd ~/app
bash deploy/update.sh ~/stock_auto_new.zip
```

`.env`와 그동안 쌓인 `data/`는 **그대로 보존**되고, 이전 코드는
`~/app.bak_날짜시각` 으로 백업됩니다.

---

## 10. 문제가 생겼을 때

| 증상 | 원인 | 해결 |
|---|---|---|
| `ModuleNotFoundError` | 가상환경을 안 켬 | `source .venv/bin/activate` |
| `python: command not found` | 가상환경을 안 켬 | 위와 동일. 또는 `python3` 사용 |
| **모든 키가 `(미설정)`** | `python-dotenv` 미설치 또는 `.env` 위치 오류 | `pip install python-dotenv`, `.env`가 `~/app/` 바로 아래인지 확인 |
| 텔레그램이 안 옴 | 봇에게 먼저 말을 안 걺 | 4-1의 4번 단계 다시 |
| `유니버스 N=20` | 위키피디아 접근 차단 | 5장 확인 |
| `추천 0건` | 시장이 약세라 게이트가 막음 | **정상입니다.** 알림의 `🌐 매크로 L0` 확인 |
| 매일 같은 종목만 | 일봉 캐시가 안 갱신됨 | `check_pipeline` 의 "일봉 신선도" 확인 |
| `No space left on device` | 디스크 참 | `du -sh ~/app/data/*` 로 확인 후 정리, 또는 디스크 늘리기 |
| 서비스가 자꾸 재시작됨 | 메모리 한도 초과 | `journalctl -u stock-auto | grep -i memory` 확인 → `bash deploy/add_swap.sh` |
| 첫 실행이 20분 넘게 안 끝남 | 일봉 500종목 최초 다운로드 | **정상입니다.** `tail -f logs/daemon.log` 로 진행 확인 |

### 로그 보는 법

```bash
tail -100 ~/app/logs/daemon.log        # 최근 100줄
grep -i "실패\|오류\|❌" ~/app/logs/daemon.log | tail -20   # 문제만 골라 보기
```

---

## 11. 보안 — 꼭 지킬 것

1. **`.env`를 zip에 넣지 마세요.** API 키가 들어 있습니다. 서버에서 직접 입력하세요.
2. `.env` 권한은 설치 스크립트가 자동으로 `600`(나만 읽기)으로 맞춥니다.
3. 키가 유출됐다고 생각되면 **즉시 각 서비스에서 재발급**하세요
   (텔레그램은 BotFather `/revoke`, Anthropic은 콘솔에서 삭제).
4. VM에 방화벽 포트를 열지 마세요. 이 프로그램은 바깥에서 접속받지 않습니다.
5. 클라우드 콘솔에서 **예산 알림**을 설정해 두면 요금 사고를 막을 수 있습니다.

---

## 12. 비용을 줄이고 싶다면

- **VM을 24시간 켤 필요가 없다면**: 실시간 폭주 감시를 포기하고 일일 배치만 돌립니다.
  systemd 대신 cron으로 하루 한 번만 실행하면 되고, 그러면 VM을 그 시간에만 켜도 됩니다.
  (다만 껐다 켜는 자동화가 따로 필요합니다.)
- **LLM 비용**: `.env`의 `STOCK_AGENT_MODEL` 을 더 저렴한 모델로 바꿉니다.
  분석 대상은 어차피 상위 15종목뿐이라 유니버스를 키워도 토큰 비용은 늘지 않습니다.
- **디스크**: 일봉 캐시는 500종목 기준 **20MB** 수준입니다. 10GB로 잡아도 남습니다.
- **머신 다운그레이드**: 실시간 폭주 감시를 포기하고 배치만 돌린다면 e2-micro(1GB)로도
  돌아갑니다(배치 최대 200MB). 다만 스왑을 반드시 만들어 두세요.

---

*본 문서는 시스템 설치 안내이며 투자 자문이 아닙니다. 이 시스템은 추천과 알림만 제공하며
주문을 실행하지 않습니다. 투자 판단과 그 결과는 본인 책임입니다.
클라우드 요금·API 정책은 수시로 바뀌므로 각 서비스의 최신 안내를 확인하세요.*
