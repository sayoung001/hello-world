# 현재 적용 로직 전수 — 무엇이 돌고 무엇이 안 도는가

> **감사 기준일**: 브랜치 `claude/trusting-heisenberg-387xeq` 최신 커밋
> **대상**: `stock_auto/` 51개 모듈 (미장 집중, KR은 `ENABLE_KR`로 대기)
> **방법**: import 추적 + 컬럼 생성/소비 전수 대조 + 진입점별 인자 전달 확인

이 문서는 "구현된 것"이 아니라 **"실제로 실행되는 것"** 을 기록한다.
구현됐지만 호출되지 않는 것은 §7에 따로 모았다.

---

## 1. 시스템 한눈에

반자동 추천 시스템. **주문을 내지 않는다.** 두 개의 독립된 실행 경로가 있다.

| 경로 | 시점 | 산출 | 진입점 |
|---|---|---|---|
| **A. 일일 배치** | US 마감 +1h (ET 17:00) | 추천 → Notion | `pipeline/run_daily_batch.py` |
| **B. 실시간 모니터** | US 개장 (ET 09:30) | 폭주 알림 → Telegram | `pipeline/run_monitor.py` |
| (통합) | 상시 | 위 둘 자동 스케줄 | `pipeline/run_daemon.py` |

두 경로는 **서로 다른 데이터·다른 판정 로직**을 쓴다.
배치는 일봉 기반 점수제, 모니터는 실시간 체결 기반 게이트제다.

---

## 2. 경로 A — 일일 배치

### 2.1 전체 흐름

```
유니버스 정의 (config/universe.py)
   ↓  US_SAMPLE 20종목 {symbol: 섹터ETF}
일봉 다운로드 (data/downloader.py)
   ↓  FinanceDataReader → 실패 시 yfinance 폴백, 400일
사전 필터 (screener._passes_prefilter)
   ↓  5일 평균 거래대금 ≥ $2M, 최소 60봉
지표 계산 (indicators/indicators_v2.calculate_indicators)
   ↓  51개 컬럼 생성 (market='US' 전달 ← 통화버그 수정)
종목 레짐 (strategy/regime_v2.add_market_regime)
   ↓  5단계 (0~4) + 전환 방향
전략 적용 (strategy/strategies.apply_all_strategies)
   ↓  Money 3 + Price 7 → Effective_Score, Final_Buy
매크로 게이트 (strategy/macro_regime)
   ↓  US500/QQQ 레짐 AND 결합
횡단면 모멘텀 (strategy/cross_sectional)
   ↓  유니버스 백분위 순위 (기록용, 판정 미반영)
상위 15 선별 → LLM 4에이전트 → Notion 게시 + 신호 적립
```

### 2.2 원천 데이터

| 항목 | 소스 | 비고 |
|---|---|---|
| 종목 일봉 OHLCV | FinanceDataReader → yfinance | 400일, CSV 캐시 |
| 지수 일봉 | 동일 | `US500`, `QQQ` |
| 종목 리스트 | `config/universe.py` `US_SAMPLE` | **20종목 하드코딩** (`--live-universe`로 FDR 상장목록 전환 가능) |
| 뉴스 | saveticker.com 크롤링 | 상위 15종목만. **현재 403 차단** — 헤더/세션 보강 필요 |

### 2.3 계산 지표 — 51개 생성, 41개 소비

`calculate_indicators()`가 만드는 컬럼과 실제 소비처. **소비 = 전략/레짐/게이트가 참조**.

#### 실제로 판정에 쓰이는 지표

| 지표 | 소비처 | 역할 |
|---|---|---|
| `EMA20` `EMA60` | 전략 6곳 | 눌림목·MACD반전·추세 기준선 |
| `MA5` `MA10` `MA20` `MA60` | 전략 + 레짐 | 정배열 판정, 레짐 5단계 |
| `adx` | 전략 3곳 + 레짐 | 추세 강도 (>20, >25) |
| `rsi_14` | 전략 7곳 | 과매도/과열 |
| `atr` | TP/SL 산출 | 목표·손절 배수 |
| `rvol` | 전략 5곳 + 레짐 | 상대 거래량 (>1.0, >1.5, >2.0) |
| `vai_stage1` `vai_stage2` | → `vai_sustained` `vai_spike_only` | **VAI 2단** 거래량 가속·지속 |
| `vai_sustained` | Money 전략 | 2단 모두 상승 = 자금 유입 지속 |
| `vai_spike_only` | Money 전략 + Penalty | 단발 펌핑 배제 |
| `liquidity_score` | 전략 + 매수판정 + Penalty | **시장별 통화 sigmoid** (US center $50M) |
| `obv` `obv_ma20` | 전략 5곳 | 자금 흐름 방향 |
| `mfi_14` | 전략 3곳 | 자금 유입 강도 |
| `macd_line` `macd_signal` `macdh` | 전략 4곳 | 반전 포착 |
| `boll_ub` `bb_pctb` `bb_width` | 전략 | 스퀴즈 돌파, 과매도 |
| `squeeze_on` | 전략 | BB+KC 수렴 상태 |
| `ichimoku_tenkan` `kijun` `senkou_a/b` | 전략 | 일목 추세 |
| `roc_12` | 레거시 Momentum | (판정 미반영 — §7.3) |
| `upper_wick_ratio` `high_close_drop` | Penalty | 윗꼬리·장중밀림 |
| `trading_value` | → `liquidity_score` | 거래대금 (Close×Volume) |
| `vol_ma20` | → `rvol` | 거래량 기준선 |
| `MA20_Break` `Bearish_Div` `Candle_BearEngulfing` | Warning_Count | 매수 차단 조건 |
| `Candle_Engulfing` `Candle_Hammer` `MA20_Bounce` | 레거시 Candle_Buy | (판정 미반영) |

### 2.4 Money 전략 3종 — "자금이 실제로 움직였는가"

| 전략 | 조건 | 가중치 | 스타일 |
|---|---|---:|---|
| `VolBreakout` | `rvol>2.0` ∧ 양봉 ∧ 20일 고가 돌파 ∧ `adx>20` ∧ **¬단발펌핑** | 2.0 | 단타 |
| `VolPumpSustained` | `vai_sustained` ∧ 양봉 ∧ 전일比 상승 ∧ `liquidity>0.7` | 2.0 | 단타 |
| `MoneyFlowSurge` | `obv>obv_ma20` ∧ `mfi_14>60` ∧ `rvol>1.5` ∧ `Close>EMA20` | 1.5 | 중단기 |

### 2.5 Price 전략 7종 — "가격 구조가 유리한가"

| 전략 | 조건 요약 | 가중치 | 스타일 |
|---|---|---:|---|
| `MeanReversion` | `rsi<30` ∧ `bb_pctb<0.1` ∧ `mfi<35` ∧ 양봉 | 1.5 | 단타 |
| `GapMomentum` | 갭업 `>1.5%` ∧ `Close≥Open` ∧ `rvol>1.0` | 1.0 | 당일~1일 |
| `PullbackBuy` | `EMA20>EMA60` ∧ EMA20 터치 후 회복 ∧ `40<rsi<60` | 1.5 | 중단기 |
| `MACDReversal` | 히스토그램 음→양 ∧ `Close>EMA60` ∧ `obv>obv_ma20` | 1.0 | 중단기 |
| `SqueezeBreakout` | 5일 중 3일 스퀴즈 → 해제 ∧ `Close>boll_ub` ∧ `rvol>1.5` | 1.5 | 중단기 |
| `IchimokuTrend` | `Close>전환선>기준선` ∧ 구름 상단 돌파 | 1.0 | 스윙 |
| `TrendContinuation` | `MA5>MA10>MA20>MA60` ∧ `adx>25` ∧ `50<rsi<70` ∧ `obv>obv_ma20` | 1.5 | 스윙 |

### 2.6 점수 합성

```
Money_Score = Σ(활성 Money 전략 × 가중치)
Price_Score = Σ(활성 Price 전략 × 가중치)

★ Money Cap:  Money_Score < 0.5 이면  Price_Score = min(Price_Score, 2.0)
   → "거래량 없이 기술지표만으로 고득점" 구조적 차단

Technical_Score = Money_Score + Capped_Price_Score

Penalty = 0.5×(윗꼬리>0.6) + 0.5×(고가→종가 낙폭>1.5%)
        + 1.0×(단발 펌핑) + 0.5×(유동성<0.5)          [상한 3.0]

Effective_Score = Technical_Score × Liquidity_Score − Penalty
```

### 2.7 최종 매수 판정

```
Final_Buy = ( Effective_Score ≥ 4.0
              OR (Effective_Score ≥ 2.5 AND Money_Score ≥ 1.5 AND liquidity_score ≥ 0.7) )
            AND Warning_Count < 2
            AND Penalty < 2.0
            AND 종목레짐 ≥ 1
            AND 매크로 게이트 통과          ← screener에서 AND 결합
            AND 섹터 게이트 통과            ← ❗현재 비활성 (§7.2)
```

`Warning_Count` = `MA20_Break` + `Bearish_Div` + `Candle_BearEngulfing`

### 2.8 게이트 2종

| 게이트 | 산출 | 차단 조건 | 상태 |
|---|---|---|---|
| **매크로 레짐** | `US500`/`QQQ` 각각 5단계 산출 → **최소값** 채택(보수적) | 종합 레짐 0 → 전 종목 차단 | ✅ 작동 |
| **섹터** | 섹터 ETF 12개를 `sector_status.compute_sector_status()`로 진단 (0~5) | 상태 0(하락위험) 종목 차단 | ✅ 작동 (`--no-sector`로 해제) |

섹터 진단은 차트·CSV 없이 상태 점수만 산출하는 경량 경로를 쓴다
(`sector/sector_status.py`). 산출된 라벨은 LLM 프롬프트와 Notion 요약에도 함께 들어간다.

### 2.9 LLM 에이전트 (상위 15종목 한정)

| 에이전트 | 입력 | 출력 | 모델 |
|---|---|---|---|
| `NewsAgent` | saveticker 헤드라인 원문 | 요약 + 센티먼트(−1~+1) + 출처 | `claude-opus-4-8` |
| `FlowAgent` | Effective/Money/RVOL/VAI/유동성/레짐 수치 | 매집·분산·단발 판단 | 동일 |
| `ValueAgent` | 종가/RSI/ADX/ATR/목표·손절/RR/섹터 | 과열·적정·저평가 | 동일 |
| `PortfolioAgent` | 위 3개 + 규칙 시그널 종합 | **추천 JSON** (진입조건·손절·목표·P10/P50/P90·매매방법·근거·출처) | 동일 + **적응형 사고** |

전부 `output_config.format` 구조화 출력. 모델은 `STOCK_AGENT_MODEL` 환경변수로 변경 가능.

---

## 3. 경로 B — 실시간 폭주 알림

### 3.1 데이터 — KIS WebSocket

TR `HDFSCNT0` (해외 실시간 체결). **실측 패킷으로 필드 전수 검증 완료.**

| 필드 | 인덱스 | 용도 |
|---|---:|---|
| 현지일자 XYMD | 4 | `ts` 산출 (ET) |
| 현지시간 XHMS | 5 | `ts` 산출 → 개장 후 경과분 |
| 시가 OPEN | 8 | 참고 |
| 현재가 LAST | 11 | 가격 이동 판정 |
| 부호 SIGN / 대비 DIFF | 12 / 13 | 전일종가 역산 → 갭 |
| 누적거래량 TVOL | 20 | **RVOL 분자** |
| 누적거래대금 TAMT | 21 | 거래대금 게이트 |
| 매도체결 ASVL | 22 | 체결강도 분모 |
| 매수체결 BIVL | 23 | 체결강도 분자 |
| 체결강도 STRN | 24 | 검증용 (매수/매도×100과 일치 확인) |

**다건 패킷**(`count>1`)은 헤더 count로 레코드 크기를 계산해 **가장 최신 레코드**만 파싱한다.

### 3.2 폭주 판정 — 4단 AND 게이트

평가 시점은 개장 후 **1 · 3 · 5분** 윈도우, 각 윈도우 도달 직후 첫 틱에서 1회 판정 (`[w, w+1)`).

```
① 거래량 (OR)   시간대 RVOL ≥ 임계   OR   Z-score ≥ 3.0
       ↓ AND
② 가격 이동      max(|시가갭|, |분당ROC|) ≥ 5.0%        (US)
       ↓ AND
③ 거래대금       누적 거래대금 ≥ $2,000,000
       ↓ AND
④ 체결강도       매수체결/매도체결 ≥ 1.2
       ↓
   🚨 Telegram 알림 (근거 문구 동봉) + 신호 적립
```

핵심은 **시간대 정규화**다. 일평균이 아니라 **같은 시간대(개장 후 경과분)의 과거 누적거래량**과 비교한다.
개장 직후는 원래 거래량이 높으므로 일평균과 비교하면 매일 전 종목이 오탐된다.

### 3.3 RVOL 임계 — 적응형

| 방식 | 산출 | 상태 |
|---|---|---|
| **적응 (기본)** | 관측 저장소의 실측 RVOL 분포 상위 1% 분위수 | ✅ `run_monitor` |
| 고정 폴백 | 표본 50건 미만이면 `RVOL ≥ 3.0` | ✅ 자동 |
| 안전 범위 | 분위수를 `2.0 ~ 12.0`으로 클램프 | ✅ |

RVOL만 대체하고 **가격이동·거래대금·체결강도 임계는 고정값 그대로 보존**한다.

### 3.4 시간대 프로파일

RVOL 분모(같은 시간대 평균 누적거래량)의 산출 방식:

| 우선순위 | 방식 | 조건 |
|---|---|---|
| 1 | **실측** — 관측 저장소의 윈도우별 누적거래량 mean/std | 10일 이상 축적 |
| 2 | **근사** — 일봉 총거래량 × 장중 분포곡선(frac) | 실측 부족 시 |
| 3 | 없음 | RVOL/Z 게이트 **비활성**, 나머지 3게이트만 평가 |

### 3.5 알림 내용

```
🚨 [거래량 폭주] AAPL (US)
⏱️ 개장 3분 · 09:33:34
💰 현재가 235.00 (갭 +4.9%)
📊 RVOL 3.5 · Z 8.6 · 체결강도 1.50
💵 거래대금 1,100,000,000
✅ RVOL=3.5≥3.0 · 가격이동=4.9%≥5.0% · 거래대금≥2,000,000 · 체결강도=1.50≥1.2
```

중복 억제: 종목별 윈도우당 1회 + 쿨다운 600초.

---

## 4. 자가보정 루프

```
[개장]  모니터가 각 윈도우에서 누적거래량 관측 → data/observations/US.csv
          (날짜는 패킷의 ET 거래일 기준 — 서버 TZ 무관)
   ↓
[마감+1h]  데몬이 관측 10일 이상 종목의 프로파일을 실측 mean/std로 재구축
   ↓
[다음날]  더 정확해진 RVOL 분모 + 적응 임계로 판정
```

정적 frac 곡선이 실측으로 점진 대체된다. 관측은 **폭주 여부와 무관하게** 전 윈도우에서 적립된다.

---

## 5. 결과 라벨링 루프

| 단계 | 내용 |
|---|---|
| **적립** | 배치 추천 + 폭주 알림 전 건을 `data/tracking/signals.csv`에 기록. 실행 여부 무관(선택 편향 방지). 피처(점수·레짐·모멘텀·ATR·RVOL)를 기록 시점에 함께 저장 |
| **라벨링** | 삼중 배리어 — 진입은 **다음 거래일 시가**, 동시 도달 시 **SL 우선**, 갭으로 배리어 관통 시 **미체결(skip)** |
| **리포트** | 성과 + **캘리브레이션 표**(점수 구간별 실제 승률) + 단조성 점검 + 소스별 알림 정밀도 |

수동 실행: `python -m stock_auto.pipeline.label_outcomes --out report.md`

---

## 6. 진입점별 실행 경로 요약

| 명령 | 하는 일 | 자동/수동 |
|---|---|---|
| `run_daily_batch --market US` | 배치 전체 + Notion + 신호 적립 | 데몬이 자동 |
| `run_monitor --market US` | 실시간 알림 + 적응임계 + 관측/알림 적립 | 데몬이 자동 |
| `run_daemon` | 위 둘을 ET 기준 cron으로 (DST 자동) | 상시 |
| `build_profiles --market US` | 프로파일 구축 (`--from-observations`로 실측 우선) | 수동/데몬 |
| `label_outcomes` | 결과 라벨링 + 리포트 | **수동 (주 1회)** |
| `preflight --live` | 키·라이브러리·외부연결 점검 | 수동 |
| `kis_capture` | 실시간 패킷 캡처 (필드 검증용) | 수동 |
| `publish_docs_to_notion` | md 문서 → Notion | 수동 |

---

## 7. ❗ 사용하지 않는 기능

### 7.1 계산되지만 판정에 안 쓰이는 지표 (7개)

`calculate_indicators()`가 매번 계산하지만 **어떤 전략·게이트도 참조하지 않는다.**

| 컬럼 | 성격 | 비고 |
|---|---|---|
| `williams_r` | 윌리엄스 %R | 계산만 |
| `stoch_d` | 스토캐스틱 %D | `stoch_k`는 `stoch_d` 계산용으로만 쓰임 → 둘 다 판정 미반영 |
| `MA120` | 120일 이평 | 계산만 |
| `close_20_ema` `close_60_ema` | `EMA20`/`EMA60` **별칭** | 기존 호환용 중복 |
| `pct_change_1d` `pct_change_5d` `pct_change_20d` | 기간 수익률 | 계산만 |

**추가로** `analyze_candlestick()`의 `Bullish_Div`, `Candle_MorningStar`도 생성 후 미소비.
(`Body` `Avg_Body` `Upper_Shadow` `Lower_Shadow`는 함수 내부 중간 계산용이라 정상)

> 제거해도 판정 결과는 바뀌지 않는다. 다만 계산 비용이 크지 않고
> 나중에 피처로 승격할 여지가 있어 **일단 유지**하는 편이 낫다.

### 7.2 배선 상태

**✅ 해결됨** — 감사에서 발견돼 이후 수정된 3건.

| 기능 | 이전 | 현재 |
|---|---|---|
| **섹터 게이트** | 진입점이 `sector_status`를 안 넘겨 완전 비활성 | `sector/sector_status.py` 신설, `run_daily_batch`·`run_daemon`이 진단 후 주입. 하락위험 섹터 종목 차단 작동 |
| **적응 임계 in 데몬** | `run_monitor`만 사용, 데몬은 고정 임계 | 데몬도 `threshold_provider` 주입. 알림 기록(`sig_store`)도 함께 연결 |
| **매도 신호(exits)** | 산출만 되고 소비처 없음 | 콘솔 출력 + Notion 요약에 "매도 신호" 섹션 추가 |

**❗ 여전히 미배선** — 의도적이거나 조건이 안 갖춰진 것.

| 기능 | 상태 | 영향 | 판단 |
|---|---|---|---|
| **횡단면 모멘텀** | 계산·기록되지만 **매수 판정에 미반영** | 순수 관찰 피처 | **의도된 설계** — 라벨 200건 후 기여도 확인하고 반영 |
| **틱 빈도 게이트** | `tick_rate_min=None` | 초당 체결 건수 미평가 | KIS 피드가 `trade_count`를 채우지 않아 사실상 불가 |
| **매물대 분석** | `check_immediate_hurdle`을 `screener`가 import만 하고 미호출, `analyze_volume_profile`은 import조차 안 함 | 매물대 저항 미반영 | 필요 시 연결 (우선순위 낮음) |

### 7.3 레거시 / 중복

| 항목 | 내용 |
|---|---|
| **레거시 전략 6종** | `Trend_Up` `Momentum` `Oversold` `Vol_Explode` `Vol_Pump` `Candle_Buy` → `Signal_Count` 합산. **`Final_Buy`는 이를 쓰지 않는다.** `sector_analysis`에서만 참조 |
| `Composite_Score` | `Effective_Score`의 복제본. 하위호환용 |
| `close_20_ema` / `close_60_ema` | `EMA20`/`EMA60` 별칭 |
| `scheduler.py` 유틸 6종 | `next_triggers` `daily_batch_trigger_kst` `kr_open_trigger_kst` `us_open_trigger_kst` `run_apscheduler` `CRON_HINTS` — **전부 외부 호출 0회**. 데몬은 APScheduler `CronTrigger`를 직접 씀. 표시/참고용 |

### 7.4 US 집중으로 대기 중 (KR 확장 경로)

`ENABLE_KR=1` 설정 시에만 활성. 코드는 유지되나 현재 미실행.

| 항목 | 상태 |
|---|---|
| `parse_kr` / TR `H0STCNT0` | 구현됨. **필드 인덱스는 명세 가정값 — 실측 미검증** |
| `KR_SAMPLE` 유니버스 8종목 | 대기 |
| KR 섹터 ETF 매핑 (`KR_SECTOR_ETF`) | 대기 |
| KR 임계값 (가격이동 3%, 거래대금 50억) | 대기 |
| KR 뉴스원 | **없음** — saveticker는 미국 전용 |

### 7.5 테스트·도구 전용 (프로덕션 미사용)

| 항목 | 용도 |
|---|---|
| `downloader.make_synthetic` | 오프라인 로직 검증용 합성 일봉 |
| `volume_monitor.ReplayFeed` | KIS 없이 모니터 로직 테스트 |
| `agents.pipeline.FakeClient` | API 키 없이 에이전트 파이프라인 테스트 |
| `profile_builder.calibrate_fraction` | 실측 분봉으로 frac 보정 — **아직 호출 안 함** |
| `tools/kis_capture.py` | 패킷 캡처 (필드 정렬 검증) |

### 7.6 외부 요인으로 막힌 것

| 항목 | 문제 |
|---|---|
| **saveticker 뉴스** | 라이브 fetch **403 (봇 차단)**. 파서는 검증됐으나 수집이 안 됨 → `NewsAgent`가 "수집된 뉴스 없음"으로 동작 |
| **Notion 실게시** | 토큰·부모페이지 필요. payload 빌더만 검증됨 |
| **LLM 라이브 호출** | `FakeClient`로 구조만 검증. 실제 응답 미검증 |

---

## 8. 요약 — 지금 실제로 결정에 영향을 주는 것

### 매수 추천을 결정하는 요소 (배치)

1. **Money 3 + Price 7 전략** 활성 여부와 가중치
2. **Money Cap** — 자금 신호 없으면 가격 점수 2.0 제한
3. **Liquidity Score** — 곱셈으로 반영 (통화별 정규화)
4. **Penalty 4종** — 윗꼬리·장중밀림·단발펌핑·저유동성
5. **Warning_Count** — 2건 이상이면 차단
6. **종목 레짐** ≥ 1
7. **매크로 레짐** (US500/QQQ 최소값) ≠ 0

### 알림을 결정하는 요소 (실시간)

1. **시간대 RVOL** (적응 분위수 또는 고정 3.0) 또는 **Z-score ≥ 3**
2. **가격 이동** ≥ 5% (갭 또는 분당 ROC)
3. **누적 거래대금** ≥ $2M
4. **체결강도** ≥ 1.2

7. **섹터 상태** ≠ 0 (하락위험 섹터 차단)

### 결정에 영향을 주지 **않는** 것

- 횡단면 모멘텀 (기록만 — 의도된 설계)
- 레거시 6전략 / `Signal_Count`
- 미소비 지표 7종 + 캔들 2종
- 매물대 분석
- 틱 빈도 게이트

---

## 9. 감사 이후 조치 결과

| 조치 | 결과 |
|---|---|
| 섹터 게이트 배선 | ✅ 완료 — `sector_status.py` 신설, 두 진입점에 주입. 검증: 하락위험 섹터 종목 `final_buy=False` |
| `run_daemon` 적응 임계 | ✅ 완료 — `threshold_provider` 주입 + 알림 기록 연결 |
| 매도 신호 출력 | ✅ 완료 — 콘솔 + Notion 요약 3섹션(섹터/점수/매도) |
| 부수 개선 | `sector_analysis`의 matplotlib·tqdm 지연 로드 → 배치가 차트 의존 없이 동작 |

미사용 지표·레거시는 **제거보다 유지**를 권한다.
계산 비용이 작고, 라벨이 쌓이면 피처로 승격할 후보들이기 때문이다.

---

*본 문서는 코드 정적 감사 결과이며, 전략의 수익성과는 무관하다.
가중치·임계값은 여전히 미검증 가정치이며 `label_outcomes` 리포트로 검증 중이다.*
