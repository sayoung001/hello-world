# 금융 분석 관점 점검 — 구조적 발견과 보완이 필요한 것

작성 2026-09-07 · 대상 `stock_auto/` 전략·평가 계층
방법: 점수 체계의 수식 전개 + 합성 일봉(150종목 × 500봉, 신호 904건) 실측

> **측정 데이터에 대한 고지.** 이 문서의 수치는 GBM 합성 일봉에서 나왔다.
> 합성 데이터에는 **설계상 알파가 없다**(일 드리프트 +0.06%뿐). 따라서 아래 수치는
> "이 전략이 얼마나 버는가"가 아니라 **"배리어·임계 구조 자체가 만들어내는 기하학"**
> 과 **귀무가설 기준선**으로 읽어야 한다. 실데이터가 이 기준선을 얼마나 넘는지가
> 곧 전략의 알파다. 실데이터 확보 후 같은 스크립트를 다시 돌려 비교할 것을 전제한다.

---

## 1. 가장 중요한 구조적 발견 — 매수는 Money 신호 없이 성립하지 않는다

### 1-1. 수식으로 증명된다

`strategy/strategies.py`의 실제 구현은 이렇다.

```
Money 전략 가중치:  VolBreakout 2.0 · VolPumpSustained 2.0 · MoneyFlowSurge 1.5   (합 5.5)
Price 전략 가중치:  MeanReversion 1.5 · GapMomentum 1.0 · PullbackBuy 1.5
                    MACDReversal 1.0 · SqueezeBreakout 1.5 · IchimokuTrend 1.0
                    TrendContinuation 1.5                                        (합 9.0)

Money Cap:   Money_Score < 0.5  →  Price_Score를 2.0으로 절단
Technical  = Money_Score + capped(Price_Score)
Liquidity  ∈ [0.4, 1.3]            (US center $50M 시그모이드)
Effective  = Technical × Liquidity − Penalty

Final_Buy  = [ Effective ≥ 4.0 ]  OR  [ Effective ≥ 2.5 AND Money ≥ 1.5 AND Liquidity ≥ 0.7 ]
```

Money 가중치의 **최솟값이 1.5**다. 즉 `Money_Score`가 가질 수 있는 값은 `0` 아니면 `≥1.5`이고,
0.5 미만이라는 것은 곧 **0**이라는 뜻이다. 그러면

```
Money = 0  →  Technical ≤ 2.0  →  Effective ≤ 2.0 × 1.3 = 2.6  <  4.0   (경로 A 불가)
              경로 B는 Money ≥ 1.5 를 명시적으로 요구                    (경로 B 불가)
```

> **Price 전략 7개(총 가중치 9.0)는 아무리 다 켜져도 단독으로는 절대 매수를 만들 수 없다.**

합성 데이터 실측에서도 `Money_Score > 0`인 매수 비율이 **100.0%** (672건 전수)로 정확히 일치한다.

### 1-2. 이것이 의도인지 확인이 필요하다

의도라면 문서에 명시해야 한다. "Money가 트리거, Price는 증폭기"는 일관된 철학이고,
"자금이 실제로 밀어줄 확률을 측정한다"는 v2.1 설계 취지와도 맞다. 문제는 **현재 구조가
그 의도를 넘어선다**는 점이다.

- Price 전략 7개는 실질적으로 **점수 가산기**일 뿐 신호원이 아니다.
  가중치 9.0을 배분하는 튜닝 작업은 **매수 여부에는 영향을 주지 않고** 순위에만 영향을 준다.
- 반대로 Money 전략 3개의 임계값은 **시스템의 유일한 관문**이다. 여기 손대면 신호 수가 통째로 바뀐다.
- 신규 전략을 Price 쪽에 추가하는 것은 **신호 수를 전혀 늘리지 못한다.**

### 1-3. 실질적으로는 MoneyFlowSurge 하나에 얹혀 있다

매수 신호 672건 중 각 전략의 발동률.

| 전략 | 축 | 발동률 |
|---|---|---:|
| **MoneyFlowSurge** | Money | **87.8%** |
| IchimokuTrend | Price | 71.0% |
| TrendContinuation | Price | 29.5% |
| VolPumpSustained | Money | 24.3% |
| PullbackBuy | Price | 17.3% |
| MACDReversal | Price | 5.4% |
| SqueezeBreakout | Price | 2.7% |
| VolBreakout | Money | 2.4% |
| MeanReversion | Price | 0.0% |
| GapMomentum | Price | 0.0% |

그리고 경로 B의 산술을 보면 지배 경로가 드러난다. Money = 1.5(MoneyFlowSurge 단독),
유동성이 좋은 종목(1.3)일 때 `Effective ≥ 2.5`가 되려면 `Technical ≥ 1.92`,
즉 `Price ≥ 0.42`면 된다 — **Price 전략 아무거나 하나면 충족**된다.

> 현재 시스템은 사실상 **"MoneyFlowSurge + 아무 추세 확인 1개"** 탐지기다.

**권고**

1. `MeanReversion` / `GapMomentum` 발동률 0%는 합성 데이터 한계일 수 있다
   (합성 시가 = 전일 종가 + 미세잡음이라 갭이 구조적으로 생기지 않는다).
   **실데이터로 반드시 재확인**해야 한다. 실데이터에서도 0%면 임계값이 잘못 잡힌 것이다.
2. `VolBreakout` 2.4%는 가중치 2.0(최고)을 받는 전략치고 지나치게 희소하다.
   희소한데 가중치가 높으면 **점수 분포에 계단이 생겨** 캘리브레이션이 어려워진다.
3. Money 축에 서로 상관이 낮은 신호를 **추가**하는 것이 신호 수를 늘리는 유일한 길이다.
   후보: 자금유입 지속성(OBV/AD선 기울기), 대량거래 상방 체결 편중,
   장중 VWAP 상단 유지 시간 비율.

---

## 2. 배리어 기하학 — 단타(3일)는 목표에 닿을 시간이 없다

합성 신호 904건에 삼중 배리어를 적용한 결과다.

```
ATR/종가 중앙값  2.77%
손절폭 중앙값    3.20%  (= 1.20 ATR)
목표폭 중앙값    5.54%  (= 2.00 ATR)   →  손익비 1.73
```

손익비 1.73의 **손익분기 승률 = 1 / (1 + 1.73) = 36.6%**.

| 보유기간 | TP | SL | 타임아웃 | 결착분 승률 | 손익분기(36.6%) 대비 |
|---|---:|---:|---:|---:|---|
| 단타 3일 | 20.2% | 38.4% | **41.4%** | **34.5%** | ▼ 미달 |
| 중단기 7일 | 35.0% | 50.8% | 14.3% | 40.8% | ▲ 초과 |
| 스윙 20일 | 42.0% | 57.0% | 1.0% | 42.5% | ▲ 초과 |

두 가지가 읽힌다.

**(a) 3일은 2 ATR 목표에 닿기에 짧다.** 신호 10건 중 4건이 결착 없이 시간이 끝난다.
타임아웃 건의 MFE는 **목표폭의 중앙값 60%까지 도달**했고, **27%는 목표의 80% 이상**까지 갔다.
가격은 움직였는데 시계가 먼저 끝난 것이다. 이건 신호의 문제가 아니라 **기간과 폭의 불일치**다.

**(b) 손절이 목표보다 먼저 닿는다.** 3일 기준 SL이 TP의 1.9배다. 1.2 ATR 손절은
일간 변동성의 잡음 구간 안에 있다 — 하루 종가 변동 1σ가 이미 2%인데 손절이 3.2%다.

**권고 (우선순위 순)**

1. **단타 horizon의 목표폭을 줄이거나 기간을 늘린다.** 3일 유지라면 목표 1.0~1.2 ATR
   (손익비 1.0 내외, 손익분기 승률 50%), 2 ATR 유지라면 기간 7일. 지금 조합은 어느 쪽도 아니다.
2. **손절을 변동성에 연동한다.** 고정 1.2 ATR이 아니라 최근 20일 실현변동성 대비
   조정하거나, 스퀴즈 구간의 저점 등 **구조적 지지선**을 쓴다.
   (코인 프로젝트 `CLAUDE.md`가 지목한 "시장 변동성 무관한 고정 SL"과 동일한 문제다.)
3. **타임아웃을 실패로 취급하지 말 것.** 현재 `label = -1`로 캘리브레이션 데이터셋에서
   제외된다. 3일 기준 41%가 제외되면 남은 표본이 SL 쪽으로 치우쳐 **점수→승률 곡선이
   비관 편향**된다. `mfe_pct / 목표폭` 을 연속형 타깃으로 함께 학습하는 편이 정보를 덜 버린다.

### 2-1. horizon을 LLM이 정하는데 라벨러가 그걸 그대로 쓴다

`labeler.HOLD_DAYS`는 기록된 `horizon`을 그대로 보유기간으로 쓴다. 그런데 그 `horizon`은
**PortfolioAgent(LLM)가 정한다.** 규칙엔진은 별도로 `styles`(전략이 스스로 규정한 성격)를
내놓는데 둘이 일치한다는 보장이 없다.

실제 구동에서 `styles=중단기`인 후보에 `horizon=단타`가 붙는 사례를 확인했다.
그러면 7일 설계의 셋업을 3일로 평가하게 되고, 위 표에 따르면 **결착 승률이
40.8% → 34.5%로 떨어져 손익분기 아래로 내려간다.** 전략이 아니라 평가 방식이 성과를 죽인다.

**권고** — `styles`를 프롬프트에 사전(prior)으로 넣고, 이탈 시 `rationale`에 근거를
쓰도록 강제한다. 그리고 기록에 `styles`와 `horizon`을 **둘 다** 남겨 사후에
"LLM이 기간을 바꾼 것이 성과를 개선했는가"를 측정할 수 있게 한다.

---

## 3. 표본이 언제 모이는가 — "약 2개월"은 유니버스 규모에 달렸다

캘리브레이션(점수→승률 로지스틱)에는 200건 이상이 필요하다는 게 `report.py`의 기준이다.
그럼 언제 모이나. 합성 데이터에서 **종목·봉당 매수 발생률은 1.99%**였다(33,720봉 중 672건).

```
종목당 연간 신호 ≈ 252 × 0.0199 ≈ 5.0건
200건 도달 개월수 ≈ 200 / (N × 5.0 / 12) = 480 / N  개월   (N = 유니버스 종목 수)
```

| 유니버스 | 200건까지 | 비고 |
|---:|---:|---|
| 20종목 | **약 24개월** | 샘플 유니버스 규모 |
| 50종목 | 약 10개월 | |
| 100종목 | 약 5개월 | |
| 300종목 | 약 1.6개월 | |
| 500종목 | 약 1.0개월 | |

여기에 **매크로 게이트와 섹터 게이트 통과율 g(<1)를 나눠야** 한다. 하락 레짐이 이어지면
신호가 통째로 0이 되는 구간이 생기므로 실제 소요는 위 값보다 길다.

> `가정에서_실측으로.md`에 적힌 "약 2개월"은 **300종목 이상 유니버스를 전제**한 값이다.
> 현재 샘플 유니버스(20종목)로 시작하면 2년이 걸린다. 이 숫자를 정정해 둔다.

**권고**

1. **첫날부터 유니버스를 300종목 이상으로 시작한다.** 규칙 스캔은 무토큰이므로 비용은
   다운로드 시간뿐이고, LLM은 상위 15개에만 붙으므로 **토큰 비용은 유니버스 크기와 무관**하다.
   유니버스를 키우지 않을 이유가 없다.
2. `top_n=15` 상한이 기록 건수를 하루 15건으로 묶는다. 다만 이 상한이 실제로 걸리는 것은
   `N × 0.0199 > 15`, 즉 **약 750종목부터**다. 300종목 규모에서는 제약이 아니다.
3. **과거 데이터로 부트스트랩할 수 있다.** 일봉은 이미 있으므로 지난 2년치 신호를 전부
   생성해 삼중 배리어로 라벨링하면 표본을 즉시 확보할 수 있다. 다만 이렇게 만든 표본은
   **당시 매크로/섹터 게이트 상태와 LLM 판단이 빠져 있으므로** 배치 신호와 동일하게
   취급해선 안 되고, `source="backfill"`로 구분해 별도 집계해야 한다.

---

## 4. 빠져 있는 것 — 금융 시스템으로서 필수인데 코드에 없는 것

### 4-1. 실적 발표일 필터 (없음 · 우선순위 최상)

코드 전체에 `earnings` 관련 로직이 **한 줄도 없다**. 이건 단순 누락이 아니라
**손실 분포의 꼬리를 통째로 방치**하는 것이다.

- 실적 발표 갭은 손절을 **건너뛴다.** 손절 3.2%를 걸어놨는데 −15% 갭다운으로 열리면
  손실은 3.2%가 아니라 15%다. 삼중 배리어 통계는 이걸 `skip`으로 처리하므로
  **리포트상 성과에서 사라지고, 실제 계좌에서만 발생한다.**
- 발동률 87.8%인 MoneyFlowSurge는 자금 유입 신호다. 실적 발표 직전 포지셔닝은
  전형적인 자금 유입이므로 **이 시스템은 실적 이벤트에 구조적으로 끌린다.**

**권고** — 원본 분석 문서 §3.6이 제안한 **D-3 차단**을 그대로 구현한다.
데이터는 yfinance `Ticker.calendar` 또는 FDR로 확보 가능하다. 차단이 아니라
경고만 할 거라면 최소한 추천 카드에 `실적 D-2` 표기가 반드시 있어야 한다.

### 4-2. 보유 종목 추적 (없음 · 우선순위 상)

`check_exit_signals`는 **스캔한 전 종목**에 대해 계산되고, `screen.exits`도
유니버스 전체의 매도 신호다. 즉 **내가 안 산 종목의 매도 신호까지 알려준다.**
반대로 **내가 산 종목이 유니버스에서 빠지면 매도 신호를 영영 못 받는다.**

반자동 시스템에서 이건 치명적이다. 진입은 추천해주는데 청산은 사용자가 기억해야 한다.

**권고** — `data/positions.csv`(종목·진입일·진입가·수량·손절·목표) 하나면 된다.
그러면 (a) 보유 종목은 유니버스와 무관하게 항상 스캔, (b) 매도 신호를 보유분만 필터링,
(c) `executed` 트리아지가 자동으로 채워짐(코드 자기반성 문서 3-3), (d) 실제 손익 집계가 가능해진다.
네 문제가 파일 하나로 동시에 풀린다.

### 4-3. 거래비용·슬리피지 (없음 · 우선순위 상)

`slippage` / `commission` 관련 코드가 **없다.** 라벨러는 목표가·손절가에
**정확히** 체결된다고 가정한다.

- TP는 `high >= tp`면 `tp`에 체결로 본다. 실제로는 지정가라면 체결이 될 수도 안 될 수도 있다.
- SL은 `low <= sl`면 `sl`에 체결로 본다. **실제 손절은 거의 항상 더 나쁘게 체결된다.**
- 미국 주식 왕복 비용은 수수료 0에 가깝더라도 스프레드+슬리피지로 소형주는 0.2~0.5%다.

손익비 1.73에서 왕복 0.3%를 빼면 실질 손익비는 `(5.54−0.3)/(3.20+0.3) = 1.50`이 되고
**손익분기 승률이 36.6% → 40.0%로 올라간다.** 위 2장 표에서 중단기 7일의 40.8%는
**비용을 넣는 순간 손익분기선에 걸친다.** 이 한 줄이 결론을 바꾼다.

**권고** — `labeler`에 `cost_bps` 파라미터(기본 30bp 왕복)를 넣어
진입가에 +비용/2, 청산가에 −비용/2를 반영한다. 리포트는 비용 전/후를 함께 보여준다.

### 4-4. 포지션 사이징 (없음 · 우선순위 중)

추천에 "얼마나 살지"가 없다. 손절가가 있으므로 리스크 기반 사이징을 계산할 수 있다.

```
수량 = (계좌 × 건당 위험비율) / (진입가 − 손절가)
```

건당 위험 0.5~1%가 표준이다. 지금은 사용자가 매번 암산해야 하고, 그 결과
**변동성이 큰 종목에 더 많은 돈이 들어가는** 전형적 실수가 발생한다
(가격이 싸 보여서 많이 사는 것과 같은 효과).

`conviction`(0~1)을 사이즈에 반영하려는 유혹이 있는데, **캘리브레이션이 끝나기 전에는
쓰지 말아야 한다.** LLM의 확신도가 실제 승률과 단조 관계인지 검증되지 않았다.
`report.py`의 캘리브레이션 표에 `conviction` 구간을 추가해 먼저 검증할 것을 권한다.

### 4-5. 종목 간 상관·집중도 통제 (없음 · 우선순위 중)

`candidates.head(top_n)`은 Effective 순 상위 15개다. **섹터 분산 제약이 없다.**
반도체가 좋은 날에는 15개 중 10개가 반도체일 수 있다. 각각은 독립적 추천처럼 보이지만
실제로는 **한 방향에 3~5배 레버리지를 건 것**과 같다.

섹터 게이트는 "나쁜 섹터를 막는" 필터이지 "좋은 섹터에 몰리는 것을 막는" 제약이 아니다.

**권고** — 상위 15개 선정 시 섹터당 최대 3개 등 간단한 제약을 건다.
`sector_etf` 컬럼이 이미 행에 있으므로 정렬 후 greedy 선택 몇 줄이면 된다.

### 4-6. 벤치마크 대비 평가 (없음 · 우선순위 중)

리포트는 승률·PF·평균수익을 절대값으로 보여준다. **같은 기간 SPY를 그냥 들고 있었다면
얼마였는지가 없다.** 상승장에서는 아무 신호나 승률 60%가 나온다.

**권고** — `report.py`에 신호 기간과 동일한 보유기간의 **SPY 동일기간 수익률**을 병기하고,
초과수익(alpha)과 정보비율을 계산한다. 매크로 레짐별 표가 이미 있으므로
레짐별 초과수익까지 보면 게이트의 실효성도 함께 검증된다.

### 4-7. 유동성 하한이 사실상 없다 (우선순위 중)

```
사전필터 하한 (settings.py):  US $2,000,000 / 5일 평균 거래대금
유동성 점수 중심 (indicators): US $50,000,000
```

**25배 차이**다. 하루 $2M 거래되는 종목은 미국 시장에서 마이크로캡이고,
$50M 중심의 시그모이드에서 유동성 점수는 하한 0.4 근처가 된다.
그러면 `Penalty += 0.5`(liquidity < 0.5)까지 붙어 매수는 거의 안 되겠지만,
**스캔 대상에는 남아** 계산 비용을 쓰고 후보 리스트를 오염시킨다.

더 중요한 건 실행 가능성이다. $2M 종목에 $10,000을 넣으면 일 거래대금의 0.5%다.
슬리피지가 4-3에서 가정한 30bp를 크게 넘어선다.

**권고** — 일봉 스크리닝의 사전필터를 **$20M~$50M**으로 올린다.
`turnover_floor`는 현재 실시간 폭주 감지의 분당 거래대금 하한($2M)과 **같은 값을 쓰고 있는데,
이 둘은 완전히 다른 척도**다(분당 vs 일평균). 설정을 분리해야 한다.

### 4-8. 메타라벨링 기준선을 잡아뒀다 (참고)

코인 프로젝트의 핵심 발견은 "SL 히트 중 80.1%가 방향은 맞았다 → 동적 SL로 개선 가능"이었다.
같은 지표를 주식 쪽 합성 데이터에서 재면 **7.0%**다(SL 459건 중 32건).

이 대비가 중요하다. **알파가 없는 데이터에서는 이 값이 7% 근처**라는 뜻이다.
실데이터에서 이 값이 50%를 넘으면 신호에 진짜 방향성 정보가 있고 손절 폭만 문제라는
코인 프로젝트와 같은 결론이 되고, 10% 근처면 **신호 자체에 방향 정보가 없다**는 뜻이다.
실데이터를 받는 즉시 가장 먼저 확인할 단일 지표로 권한다.

---

## 5. 우선순위 정리

| 순위 | 항목 | 근거 | 위치 |
|---|---|---|---|
| 1 | 실데이터로 4-8 지표(SL 중 방향정확률) 측정 | 전략의 존재 이유를 가르는 단일 지표 | 4-8 |
| 2 | 실적 발표일 D-3 차단 | 손실 꼬리를 통째로 방치 중 | 4-1 |
| 3 | 거래비용 반영 | 중단기 결론이 손익분기선에서 뒤집힘 | 4-3 |
| 4 | 유니버스 300종목 이상 | 표본 24개월 → 1.6개월 | 3 |
| 5 | 보유 종목 파일 | 청산 신호·트리아지·실손익이 한 번에 해결 | 4-2 |
| 6 | 단타 목표폭/기간 재조정 | 41%가 결착 없이 종료 | 2 |
| 7 | 유동성 사전필터 상향 + 설정 분리 | 실행 불가능한 종목이 스캔에 포함 | 4-7 |
| 8 | 섹터 집중도 제약 | 15개 추천이 사실상 1개 베팅일 수 있음 | 4-5 |
| 9 | 벤치마크 대비 초과수익 | 상승장 착시 제거 | 4-6 |
| 10 | 리스크 기반 사이징 | 캘리브레이션 이후 conviction 반영 | 4-4 |

1~3번은 **실데이터 확보 즉시** 착수 가능하고, 4~5번은 지금 당장 코드로 할 수 있다.
6번 이후는 표본이 쌓여야 판단 근거가 생긴다.

---

## 6. 검증 재현 방법

이 문서의 수치는 아래 절차로 재현한다. 실데이터를 붙일 때 같은 절차를 그대로 쓰면
합성 기준선과 직접 비교할 수 있다.

1. 유니버스 일봉 로드 → `calculate_indicators(df, market="US")`
2. `add_market_regime` → `detect_regime_transition` → `apply_all_strategies`
3. `Final_Buy == True` 인 봉마다 `calculate_strategy_levels`로 TP/SL 산출
4. `tracking.labeler.label_record(rec, df, hold_days=3|7|20)` 로 삼중 배리어 적용
5. TP/SL/타임아웃 분포, 결착분 승률, 손익분기 승률(=1/(1+손익비)) 비교
6. SL 건 중 `direction_correct == 1` 비율 산출 → 4-8 지표
