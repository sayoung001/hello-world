# 코드 자기반성 점검 — 연결이 끊긴 곳과 추가해야 할 곳

작성 2026-09-07 · 대상 `stock_auto/` 전체 · 방법: 정적 독해 + 실제 구동(합성 일봉) 검증

이 문서는 "잘 만들었다"를 확인하는 문서가 아니라, **내가 만든 코드가 실제로 매일 돌았을 때
조용히 틀리는 지점**을 찾아 적은 문서다. 특히 사용자가 지적한 두 가지에 집중했다.

1. 모듈끼리 **날짜·시간이 어긋나는 곳**
2. 실제로 돌렸을 때 **추천 산출물이 성립하는가**

각 항목은 `발견 → 왜 위험한가 → 조치` 순서다. `[수정함]`은 이번 커밋에 반영,
`[남음]`은 다음 작업으로 남긴 것이다.

---

## 0. 요약

| # | 항목 | 심각도 | 상태 |
|---|---|---|---|
| 1 | 일봉 캐시가 갱신되지 않아 **매일 같은 날 데이터로 분석** | 치명 | 수정함 |
| 2 | 신호 기록 날짜가 '실행일' → 라벨러 전진구간이 비어 **영구 미라벨** | 치명 | 수정함 |
| 3 | LLM이 낸 손절/목표가 **무검증 통과** → 전 건 미체결(skip) 처리 | 치명 | 수정함 |
| 4 | 추천-후보 매칭을 **티커 문자열**로 → 계획 필드 조용한 유실 | 높음 | 수정함 |
| 5 | 라벨링이 데몬에 **연결 안 됨** → 자가보정 루프가 영원히 안 돎 | 높음 | 수정함 |
| 6 | 알림 기록에 TP/SL이 없어 **전 건 타임아웃** → 알림 정밀도 측정 불가 | 높음 | 수정함 |
| 7 | `labeler`가 첫 기록의 시장을 전 종목에 적용 | 중간 | 수정함 |
| 8 | `sector_analysis.TODAY`가 **임포트 시점에 고정** | 중간 | 수정함 |
| 9 | `scheduler.py`(휴장일 로직)가 **어디서도 import되지 않음** | 중간 | 남음 |
| 10 | `executed` 트리아지 입력 경로 없음 → 리포트 5장 영구 미출력 | 중간 | 남음 |
| 11 | `check_immediate_hurdle` 스크리너에서 import만 하고 미사용 | 낮음 | 남음 |
| 12 | `store.append`가 매 호출마다 CSV 전량 재독 | 낮음 | 남음 |

---

## 1. 날짜·시간 충돌 점검 (사용자 지적 사항)

### 1-1. `[수정함]` 일봉 캐시가 영원히 갱신되지 않았다 — 가장 치명적

`data/downloader.py` `load_or_download()`는 캐시 파일이 **존재하기만 하면** 그대로 돌려줬다.
요청한 `start`/`end`는 캐시 적중 시 아무 역할도 하지 못했다.

```python
# 수정 전
if use_cache and path.exists():
    return pd.read_csv(path, index_col=0, parse_dates=True)   # start/end 무시
```

`pipeline/daily_batch.py::_load_map`은 `use_cache` 기본값(True)으로 호출한다. 즉

> **첫날 받은 일봉으로 둘째 날도, 셋째 날도, 1년 뒤에도 분석한다.**

가장 나쁜 점은 **실패가 조용하다**는 것이다. 배치는 정상 종료하고, 후보도 나오고,
추천 문구도 그럴듯하다. 다만 내용이 매일 똑같다. 운영자가 "오늘도 같은 종목이네"라고
느끼기까지 수 주가 걸릴 수 있고, 그 사이 쌓인 기록은 전부 폐기 대상이다.

**조치** — '지금 존재해야 할 마지막 봉'을 계산해 캐시의 마지막 봉과 비교한다.
낡았으면 재다운로드 후 캐시와 병합(과거는 캐시, 겹치는 날짜는 신규 우선)한다.

```python
def expected_last_bar(market, end=None) -> pd.Timestamp:
    # 거래소 현지시각 기준. 마감(US 16:00 / KR 15:30) 전이면 오늘 봉은 아직 없다.
    # 주말은 직전 금요일로 되감는다.
```

공휴일 달력은 일부러 넣지 않았다. 공휴일에는 이 값이 실제 마지막 봉보다 하루 앞서
**불필요한 다운로드가 1회** 발생하지만, 낡은 데이터를 그대로 쓰는 위험보다 훨씬 싸다.

시작일 비교에는 10일 여유를 뒀다. `start = 오늘 - 400일`은 매일 하루씩 밀리므로
엄격 비교하면 캐시가 매일 무효화되어 500종목 배치가 감당이 안 된다.

**검증**

```
낡은 캐시(마지막 봉 2026-08-01) → 재다운로드 1회, 병합 후 마지막 봉 2026-09-04
신선한 캐시 + 하루 밀린 start   → 추가 다운로드 0회 (캐시 재사용)
expected_last_bar(US)          = 2026-09-04  (월요일 장중 → 직전 금요일)
expected_last_bar(US, 토요일)  = 직전 금요일
```

### 1-2. `[수정함]` 신호 기록 날짜 = 실행일 → 라벨러가 영원히 결과를 못 붙인다

`daily_batch.run_daily()`는 `date = market_today(market)`(=오늘, ET)로 기록했다.
그런데 판단의 근거는 **마지막 일봉**이고, 배치 시점(마감+1h)에 오늘 봉이 아직
데이터 제공사에 올라오지 않았으면 마지막 봉은 어제다.

`tracking/labeler.py`는 이렇게 전진 구간을 자른다.

```python
fwd = df[df.index > sig_date]
if len(fwd) < 2: return Outcome(exit_type="nodata")
```

기록일이 실제 봉보다 미래면 `fwd`가 비고, 다음 날 다시 라벨링해도 또 비고,
**영구 `nodata`**가 된다. 실제 구동에서 이 어긋남을 확인했다.

```
result.date = 2026-09-07   (실행일, ET)
screen.as_of = 2026-09-04   (판단 근거 봉)
```

**조치** — 세 곳을 손봤다.

- `strategy/screener.py` — 종목 행마다 `bar_date`(그 종목의 마지막 봉 날짜)를 담고,
  `ScreenResult.as_of`(전 종목 최댓값)를 노출한다.
- `pipeline/daily_batch.py::_record_signals` — 기록 날짜를 `row["bar_date"]`로 쓴다.
- Notion 게시 — 추천 행의 '일자'를 `as_of`로, 요약 페이지 제목은
  `[US] 2026-09-07 분석 (기준봉 2026-09-04)`처럼 둘이 다를 때만 병기한다.

**부수 효과(좋은 쪽)** — `signal_id = sha1(date|symbol|source|window)`이므로
공휴일에 배치가 돌아도 기준봉이 같아 **같은 id가 되어 중복 기록이 자동으로 걸러진다.**
기존 방식이었다면 공휴일마다 같은 봉에 대한 중복 신호가 매일 새로 쌓였다.

### 1-3. `[수정함]` `sector_analysis.TODAY`가 임포트 시점에 고정

```python
TODAY = datetime.now().strftime('%Y-%m-%d')          # 모듈 최상단
BASE_DIR = f"./Results/Sector_Analysis/{TODAY}"
```

상시 구동 데몬은 모듈을 **한 번만** 임포트한다. 즉 가동 첫날 날짜가 그대로 굳어
이후 모든 차트·요약 CSV가 첫날 폴더에 덮어써지고, 텔레그램 브리핑 제목도 첫날 날짜로 나간다.
서버 로컬 시각(KST)을 쓴 것도 거래소 기준과 어긋난다.

**조치** — `today_str()` / `base_dir()` / `good_dir()` / `bad_dir()` / `summary_file()`
함수로 바꿔 **호출 시점에 ET 기준으로** 계산한다(사용처 12곳 일괄 치환).

> 현재 데몬 경로는 경량 `sector/sector_status.py`를 쓰므로 이 버그는 **잠복 상태**였다.
> 차트/CSV 리포트를 켜는 순간 드러났을 문제라 미리 막았다.

### 1-4. `[수정함]` 라벨 시각이 서버 로컬 시각

`labeler.label_pending`의 `labeled_at`이 TZ 표기 없는 서버 로컬 시각이었다.
감사 추적 값이므로 `now_et()` + `%Z`로 바꿨다.

### 1-5. `[문제 없음]` 확인한 것들

- **모니터 세션 길이** `datetime.now() + timedelta(minutes=390)`는 '경과 시간'이라
  TZ 무관하다. US 09:30–16:00, KR 09:00–15:30 모두 정확히 390분.
- **APScheduler** 기준 TZ를 ET로 두고 KR 잡만 `timezone=KST`를 명시 → DST 자동 처리.
- **US 배치(17:00 ET)와 KR 모니터(09:00 KST)** 는 시각이 겹치지 않는다.
- **실시간 관측/알림의 날짜**는 이미 `snap.ts`(패킷 XYMD+XHMS에서 만든 ET 시각) 기준이라
  서버 TZ와 무관하다.

### 1-6. `[남음]` 아직 남은 시간 관련 위험

- **반장(early close) 미대응.** 추수감사절 다음날·크리스마스 이브는 13:00 ET 마감인데
  모니터는 390분(=16:00까지) 돈다. 3시간 동안 빈 시장에 재접속을 반복한다.
  기능적 오류는 아니지만 로그를 더럽히고 API 호출을 낭비한다.
- **휴장일 미대응.** `_recalibrate_and_batch`는 `mon-fri`로만 걸러 공휴일에도 돈다.
  1-2 수정 덕에 데이터가 오염되지는 않지만, 불필요한 다운로드와 LLM 호출이 발생한다.
  → 9번 항목(`scheduler.py` 미연결)과 같은 뿌리다.

---

## 2. "실제로 돌렸을 때 추천이 성립하는가" 검증

합성 일봉 3종목(매수 신호가 실제로 서는 종목만 선별)으로 배치 → 기록 → 라벨링
전 구간을 돌렸다. **여기서 치명적인 문제 두 개가 더 나왔다.**

### 2-1. `[수정함]` LLM의 손절/목표가가 무검증으로 통과한다

첫 구동 결과.

```
BUY0 horizon='단타' reco='BUY' stop=0.0 target=0.0 atr=1.93 eff=3.9
라벨 통계: {'labeled': 0, 'skip': 3}
```

원인 사슬:

1. `RECO_SCHEMA`의 `stop`/`targets`는 `number` — **0도, 음수도, 종가 반대편 값도 스키마 통과.**
2. `store.from_screen_row`가 `reco["stop"]`을 규칙엔진 값보다 **우선** 채택 → `stop=0`.
3. `labeler`의 갭 통과 방어가 `entry >= tp`(126 ≥ 0)를 참으로 보고 **전 건 `skip`**.

즉 LLM이 숫자 하나만 이상하게 내면 **캘리브레이션 표본이 0건**이 되고,
리포트에는 "결과 대기 0건, 미체결 3건"만 남아 원인을 추적하기 어렵다.
Notion에는 손절 0원이 그대로 게시된다.

**조치** — `agents/pipeline.py`에 `reconcile_levels(reco, row)`를 만들어
에이전트 출력 직후 한 번 걸러낸다. 판정 기준은 종가 대비 상식선이다.

| 필드 | 유효 조건 | 위반 시 |
|---|---|---|
| `stop` | `0 < stop < close` | 규칙엔진 `row["stop"]`으로 복구 |
| `targets` | `t > close` 인 것만 | 전부 탈락 시 `row["target"]`으로 복구 |
| `entry_zone` | `0.7×close ≤ z ≤ 1.3×close` | 전부 탈락 시 `[close]` |

```
입력 {'stop': 0.0,   'targets': [0.0]}  → {'stop': 95.0, 'targets': [110.0], 'entry_zone': [100.0]}
입력 {'stop': 150.0, 'targets': [90.0]} → {'stop': 95.0, 'targets': [110.0], 'entry_zone': [100.0]}   # 뒤집힌 값
입력 {'stop': 97.0,  'targets': [105,112]} → 그대로 통과
```

`store.from_screen_row`의 폴백도 함께 고쳤다. `dict.get(key, default)`는
**키가 존재하면 값이 `None`이어도 default를 쓰지 않는다** — 정상적으로 보이지만
"LLM이 키는 냈는데 값이 비었을 때" 규칙엔진 값으로 못 돌아가는 함정이었다.

### 2-2. `[수정함]` `horizon`이 빈 값으로 기록된다

같은 구동에서 `horizon=''`이 기록됐다. `_record_signals`가 추천을 **티커 문자열**로 매칭했기 때문이다.

```python
by_ticker = {r.get("ticker"): r for r in result.agents.recommendations}
... by_ticker.get(row.get("symbol"))     # 티커가 1글자라도 다르면 None
```

LLM이 `AAPL ` (뒤 공백), `aapl`, 회사명 등을 내면 매칭이 실패하고
`recommend`·`horizon`·`conviction`·`entry_rule`·`stop`·`target`·P10~P90이
**전부 빈 값으로 조용히 기록된다.** 그리고 빈 `horizon`은 라벨러에서
`DEFAULT_HOLD=5`로 조용히 대체되므로, 단타(3일) 추천이 5일 보유로 평가된다.
"틀린 줄도 모르고 틀린 성과표"가 나온다.

**조치** — `run_agents`는 `candidates`를 **입력 순서대로 순회하며 append**하므로
i번째 추천이 i번째 후보다. 순서 기반 매칭으로 바꾸고, 길이가 모자랄 때만 티커로 폴백한다.

### 2-3. `[수정함]` 폭주 알림은 라벨이 붙어도 전부 타임아웃이었다

`from_surge_signal`은 `stop`/`target`/`atr`을 기록하지 않는다(실시간 스냅샷에 ATR이 없다).
`_barriers()`는 셋 다 없으면 `(None, None)`을 돌려주고, 배리어가 없으면
**모든 알림이 `timeout`(label=-1)** 이 된다. 리포트의 캘리브레이션 데이터셋은
TP/SL만 쓰므로 **알림은 학습 표본에서 통째로 빠진다.**

사용자 정책상 "알림 정밀도"는 재현율보다 우선 관리 지표인데, 정작 그 수치를 못 만든다.

**조치** — `_barriers()`에 신호일까지의 일봉(`df[df.index <= sig_date]`)을 넘겨
기록에 ATR이 없으면 **그 자리에서 ATR(14)를 계산**해 배리어를 만든다.
신호일 이전 데이터만 쓰므로 look-ahead가 없다. 다운로드 시작일도 60일 앞으로 넉넉히 당겼다.

**검증 (수정 후 전 구간)**

```
result.date=2026-09-07  screen.as_of=2026-09-04
BUY1 date=2026-09-04 horizon='단타' reco='BUY' stop=112.31 target=122.61 eff=5.34
BUY0 date=2026-09-04 horizon='단타' reco='BUY' stop=170.85 target=188.30 eff=4.20
BUY2 date=2026-09-04 horizon='단타' reco='BUY' stop=163.12 target=181.82 eff=3.69

라벨: {'labeled': 3, 'pending': 0, 'nodata': 0, 'skip': 0}
  BUY1 exit=tp      ret=+5.40% days=3 label=1
  BUY0 exit=timeout ret=-1.90% days=3 label=-1
  BUY2 exit=timeout ret=+0.49% days=3 label=-1
알림: {'labeled': 1} → ALERT BUY0 exit=timeout ret=-2.14% label=-1
```

`horizon='단타'` → 보유 3일 → `days=3`에 타임아웃. 의도대로 동작한다.

### 2-4. `[수정함]` 매크로 레짐 표에 빈 라벨 `L`이 찍혔다

알림 기록에는 `macro_regime`이 없어 리포트가 `L`(뒤가 빈)로 출력했다. `미상`으로 바꿨다.

---

## 3. 연결이 끊긴 경로 (이어야 할 곳)

### 3-1. `[수정함]` 라벨링이 데몬에 연결되어 있지 않았다

`pipeline/label_outcomes.py`(CLI)는 존재하지만 `run_daemon.py` 어디서도 호출하지 않았다.
결과: **신호는 매일 쌓이는데 결과가 영원히 안 붙는다.** 점수→승률 캘리브레이션 표는
빈 채로 남고, "자가보정 시스템"이라는 설계 전제가 성립하지 않는다.

**조치** — `_recalibrate_and_batch()` 맨 앞에 ⓪단계로 넣었다.
배치 **전에** 돌리는 이유는, 전일까지의 결과가 확정된 상태에서 그날 배치를 돌려야
리포트와 신규 신호의 기준일이 어긋나지 않기 때문이다.

```
[daemon] 결과 라벨링: 신규 12건 · 대기 34건 · 미체결 1건
[daemon] US 프로파일: 실측 18 / frac 2
[daemon] US 섹터 게이트: 12개 진단
[batch]  US 매크로 L3(상승초기) | 후보 7 | 실패 0
```

### 3-2. `[남음]` `pipeline/scheduler.py`가 아무 데서도 import되지 않는다

휴장일 집합을 받는 `_is_business_day()`, DST를 고려한 `daily_batch_trigger_kst()`가
구현되어 있는데 **사용처가 0곳**이다. `run_daemon.py`는 APScheduler `CronTrigger`를
직접 쓰고 `day_of_week="mon-fri"`로만 거른다.

- 지금 필요한 것: 미국 시장 휴장일 집합(연 1회 갱신, 또는 `exchange_calendars` 의존 추가)
- 연결 지점: `_recalibrate_and_batch()`와 `_run_monitor_thread()` 진입부에서
  `_is_business_day(now_et(), US_HOLIDAYS)` 확인 후 조기 반환
- 반장(1-6)도 같은 자리에서 `session_minutes`를 210분으로 줄이면 해결된다

### 3-3. `[남음]` `executed`(실행 여부) 입력 경로가 없다

`store.set_executed(signal_id, executed)`는 있는데 이를 호출하는 CLI/UI가 없다.
`SignalRecord.executed`는 영구히 빈 문자열이고, 리포트 **5장 "실행 여부별 (선택 편향 점검)"**
은 조건부라 영원히 출력되지 않는다(그래서 리포트 목차가 4장 → 6장으로 건너뛴다).

이건 단순 누락이 아니라 **설계 의도의 핵심**이 빠진 것이다. 반자동 시스템에서
"사람이 고른 것과 고르지 않은 것의 성과 차이"는 사람의 개입이 알파를 더하는지
빼는지를 재는 유일한 수단이다.

- 제안: `python -m stock_auto.pipeline.triage` — 미분류 기록을 하나씩 보여주고
  `y/n/s`를 받아 `set_executed` 호출. 또는 텔레그램 인라인 버튼(콜백)으로 즉시 입력.

### 3-4. `[남음]` `check_immediate_hurdle`이 스크리너에서 import만 되고 미사용

`strategy/screener.py:26`에서 import하지만 호출하지 않는다.
`sector/sector_analysis.py:261`에서는 실제로 쓴다(매물대 저항 점수).
종목 스크리닝에도 매물대 저항은 유효한 감점 요소이므로, 쓰든지 import를 지우든지 정해야 한다.
→ 금융 관점 판단이 필요해 `금융분석_관점_점검_및_보완필요.md` 4-3에서 다룬다.

### 3-5. `[남음]` Notion 추천 DB 최초 생성 경로가 CLI에 없다

`NotionPublisher.create_recommendations_db(parent_page_id)`는 1회 셋업용인데
호출하는 진입점이 없다. `.env`의 `NOTION_RECO_DB_ID`를 사람이 채워야 하는데
그 ID를 얻는 방법이 코드 안에 없다. `tools/preflight.py --live`에
"DB 없으면 생성 후 ID 출력" 한 단계를 넣는 게 자연스럽다.

---

## 4. 성능·견고성 (급하지 않지만 기록)

### 4-1. `store.append`가 매 호출마다 CSV를 전량 재독한다

```python
existing = {r["signal_id"] for r in load(base)} if p.exists() else set()
```

장중 알림마다 호출되므로 누적 행 수에 대해 O(n²)다. 하루 수십 건·연 수천 행 규모면
체감 문제는 없지만(1만 행 CSV 재독 ≈ 수 ms), 유니버스를 500종목으로 키우고
알림이 분당 여러 건 나오면 다시 볼 지점이다. 해결책은 (path, mtime, size) 키의
id 집합 캐시.

### 4-2. 스레드 간 CSV 동시 쓰기 보호가 없다

`run_daemon`은 모니터를 스레드로 띄운다. `ENABLE_KR=1`로 US·KR 모니터가 동시에
뜨면 두 스레드가 같은 `signals.csv`에 append한다. `load` → 필터 → `append` 사이에
락이 없어 이론상 행 유실이 가능하다. 현재 US 전용 운영에서는 발생하지 않는다.
KR을 켜는 시점에 파일 락 또는 시장별 파일 분리가 필요하다.

### 4-3. `strategies.py` 모듈 docstring이 구현과 달랐다 `[수정함]`

최상단은 `Effective_Score = Technical × Liquidity × (1 - Penalty)`,
구현과 내부 docstring은 `Technical × Liquidity - Penalty`. 원본에서 넘어온 불일치라
구현 기준으로 정정했다. 곱셈으로 오해하면 Penalty의 영향력을 과소평가하게 된다
(감산은 점수가 낮을수록 상대적 타격이 크다).

---

## 5. 이번 커밋에서 바뀐 파일

| 파일 | 변경 |
|---|---|
| `data/downloader.py` | `expected_last_bar()` 추가, 캐시 신선도 판정 + 병합 |
| `strategy/screener.py` | 행에 `bar_date`, `ScreenResult.as_of` 추가 |
| `pipeline/daily_batch.py` | 기록 날짜를 기준봉으로, 추천 매칭을 순서 기반으로, Notion 일자/제목 |
| `agents/pipeline.py` | `reconcile_levels()` 추가 — LLM 손절/목표/진입구간 검증 |
| `tracking/store.py` | `None` 값일 때 규칙엔진 값으로 폴백 |
| `tracking/labeler.py` | ATR 폴백 산출, 종목별 시장, 라벨 시각 ET |
| `tracking/report.py` | 빈 매크로 레짐 라벨 `미상` |
| `pipeline/run_daemon.py` | ⓪ 결과 라벨링 단계 연결 |
| `sector/sector_analysis.py` | 날짜 상수 → 호출 시점 함수(ET) |
| `strategy/strategies.py` | 모듈 docstring 수식 정정 |

---

## 6. 다음 작업 순서 제안

1. **휴장일 캘린더 연결** (3-2) — 데이터 오염은 막았지만 낭비와 로그 오염이 남아 있다.
2. **트리아지 입력 경로** (3-3) — 표본이 쌓이기 시작하는 지금 넣어야 소급이 가능하다.
   나중에 넣으면 그 전 기록은 영영 `executed` 미상으로 남는다.
3. **Notion DB 부트스트랩** (3-5) — 첫 배포 직전에 필요.
4. **동시 쓰기 보호** (4-2) — KR을 켜기 전까지는 보류 가능.

> 기록 누락은 소급 복구가 되지 않는다. 2번을 미루면 미룬 기간만큼 데이터가 영구히 빈다.
